# Development dependencies for testing ACME Certificate Manager Lambda
# Install with: pip install -r requirements-dev.txt

# Pin test tooling so promote-to-prod Lambda tests are reproducible across re-runs.
pytest==9.1.1

# AWS SDK (provided by Lambda runtime, needed for testing).
# Kept in lockstep with custom-domain-cert/lambda/requirements-test.txt: CI
# installs both cert-lambda test requirement sets into one env, so divergent
# boto3/botocore pins here would only ever be shadowed (last-write-wins).
boto3==1.43.67
botocore==1.43.67

# The Lambda dependencies (for running tests)
-r requirements.txt
