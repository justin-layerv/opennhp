# Test-only dependencies for Status Aggregator Lambda unit tests.
# Install with: pip install -r requirements-dev.txt
#
# Pin versions explicitly (matching the other Lambda test suites) so CI
# runs are reproducible across re-triggers. AWS calls are stubbed with
# unittest.mock, so no moto dependency is needed.

pytest==9.1.1
boto3==1.43.71
botocore==1.43.71
