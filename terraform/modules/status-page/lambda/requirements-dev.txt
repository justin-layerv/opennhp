# Development dependencies for testing Status Aggregator Lambda
# Install with: pip install -r requirements-dev.txt

# Testing framework
pytest>=8.4.2

# AWS SDK (provided by Lambda runtime, needed for testing)
boto3>=1.43.46

# Mocking AWS services
moto>=5.0.0
