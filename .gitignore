# ============================================================================
# Go
# ============================================================================

# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib
.*
!.github/
!.gitattributes
!.golangci.yml
!.pre-commit-config.yaml

# Claude Code
!.claude/
.claude/*
!.claude/commands/

# NHP binaries (compiled without extension on Unix)
nhp-server
nhp-acd
nhp-agent
endpoints/nhp-acd
endpoints/nhp-server
endpoints/nhp-agent
# Example plugin binary (produced by `go build ./...` in examples/server_plugin)
examples/server_plugin/server_plugin

# Test binary, build with `go test -c`
*.test

# Coverage
*.out
coverage.html
coverage.txt

# Go workspace (go.sum should be committed)
go.work
go.work.sum

# Vendor directory (if not vendoring)
# vendor/

# ============================================================================
# Terraform
# ============================================================================

# Local .terraform directories
**/.terraform/

# State files
*.tfstate
*.tfstate.*
*.tfstate.backup

# Plan files
*.tfplan
*.tfplan.json
tfplan
tfplan.*

# Variables files with secrets (but not example files)
*.tfvars.json
*.auto.tfvars
!*.example.tfvars

# Crash logs
crash.log
crash.*.log

# Override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CLI config
.terraformrc
terraform.rc

# Lock file (commit this for consistent provider versions)
# .terraform.lock.hcl

# Lambda zips (generated by archive_file)
terraform/**/*.zip

# Lambda build artifacts (keep structure, ignore contents)
terraform/**/build/package/*
!terraform/**/build/package/.gitkeep
terraform/**/.build/

# ============================================================================
# IDE / Editors
# ============================================================================

# JetBrains (IntelliJ, GoLand, etc.)
.idea/
*.iml
*.ipr
*.iws
out/

# VSCode
.vscode/
*.code-workspace

# Vim
*.swp
*.swo
*.swn
*~

# Emacs
*~
\#*\#
.#*

# Sublime Text
*.sublime-project
*.sublime-workspace

# ============================================================================
# OS
# ============================================================================

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# Windows
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk

# Linux
*~
.fuse_hidden*
.nfs*

# ============================================================================
# Build / Release
# ============================================================================

# Build output (root level only - not nested build/ dirs in terraform modules)
/build/
dist/
release/
bin/

# Smoke suite run artifact (written by scripts/run-smoke.sh's tee; consumed by
# CI steps in-job, never committed).
tests/smoke/test-output.txt

# Debug
debug
*.dSYM/

# ============================================================================
# Docker
# ============================================================================

# Logs
docker/*/logs/
**/logs/*.log

# Local overrides
docker-compose.override.yml
docker-compose.local.yml

# ============================================================================
# Secrets / Credentials
# ============================================================================

# Environment files
.env
.env.*
!.env.example

# Private keys
*.pem
*.key
*.p12
*.pfx
id_rsa
id_ed25519

# Generated local/demo TLS material
docker/nhp-agent/etc/certs/
endpoints/agent/main/etc/certs/

# AWS
.aws/
credentials

# Plugin config files (may contain secrets)
**/plugins/*/etc/config.toml
!**/plugins/*/etc/config.example.toml

# Secrets
secrets/
*.secret
*.secrets

# ============================================================================
# Logs / Temp
# ============================================================================

# Logs
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Temp files
tmp/
temp/
*.tmp
*.temp
*.bak

# ============================================================================
# Dependencies
# ============================================================================

# Node (if any JS tooling)
node_modules/
package-lock.json
yarn.lock

# Python (if any Python tooling)
__pycache__/
*.py[cod]
*$py.class
.Python
*.egg-info/
.eggs/
venv/
.venv/

# ============================================================================
# Testing
# ============================================================================

# Coverage reports
coverage/
htmlcov/
.coverage
.coverage.*

# Test results
test-results/
junit.xml
report.xml

# ============================================================================
# Misc
# ============================================================================

# Archives (usually generated)
*.tar.gz
*.tgz
*.rar

# Compiled files
*.o
*.a
*.lib

# Core dumps (use /core to avoid matching nhp/core/ directory)
/core
/core.*
