# Fail: same shape as pass-allowlisted-code-group, annotation removed — proves annotation is load-bearing.
on:
  push:
    branches: [main]
    paths:
      - 'nhp/**'
      - '.github/workflows/build-and-push.yml'
jobs:
  changes:
    runs-on: ubuntu-latest
    steps:
      - uses: dorny/paths-filter@fbd0ab8f3e69293af611ebaee6363fc25e6d187d
        with:
          filters: |
            code:
              - 'nhp/**'
              - '.github/workflows/**'
            app:
              - 'nhp/**'
