//go:build ignore

package fixture

/*
 * Doc block above the real declaration. The Go convention is for
 * continuation lines to start with a `*` after whitespace.
 *
 * redirectURLField = "old_name_pre_refactor"
 */
const (
	redirectURLField = "redirect_url"
)
