# Route53 narrow-condition fixture: no data sources needed. The condition lint
# should allow wildcard hosted-zone resources when every record name must match
# a non-wildcard normalized-name pattern.
