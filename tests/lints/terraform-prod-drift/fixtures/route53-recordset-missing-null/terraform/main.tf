# Route53 missing-null fixture: no data sources needed. The condition lint
# should fail when a wildcard hosted-zone grant has a narrow ForAllValues
# normalized-name condition but no Null=false absent-key guard.
