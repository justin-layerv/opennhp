# KMS wildcard-decrypt (#1523): a ForAllValues:StringEquals aws:ResourceAccount
# scope is a valid same-account bind once the set-qualifier prefix is stripped.
# Exercises the ForAllValues:/ForAnyValue: prefix-strip branch. Must NOT be
# flagged.
