# Route53 partial-interpolation fixture: no data sources needed. The condition
# lint should reject unresolved interpolations embedded inside otherwise-literal
# record names because it cannot prove their runtime suffix.
