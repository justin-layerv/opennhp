# Route53 unreviewed-variable fixture: no data sources needed. The condition
# lint should fail wildcard hosted-zone policies that defer normalized record
# names to an arbitrary variable without a reviewed Terraform validation guard.
