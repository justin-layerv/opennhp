# Route53 ACME-boundary fixture: TXT-only ACME record mutations are allowed
# through a wildcard hosted-zone ARN only when all multi-valued Route53 keys
# have narrow ForAllValues conditions plus Null=false absent-key guards.

