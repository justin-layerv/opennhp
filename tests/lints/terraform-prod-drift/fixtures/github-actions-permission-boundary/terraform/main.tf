# GitHub Actions permission-boundary fixture: no data sources needed. The
# condition lint should reject a permissions boundary on the canonical
# terraform CI role because the security-module boundary is ACME TXT-only.
