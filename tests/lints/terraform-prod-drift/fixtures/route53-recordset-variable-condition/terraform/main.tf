# Route53 variable-condition fixture: no data sources needed. The condition
# lint should keep accepting the real module shape where Terraform variables
# supply the normalized record names and validation is the runtime guard.
