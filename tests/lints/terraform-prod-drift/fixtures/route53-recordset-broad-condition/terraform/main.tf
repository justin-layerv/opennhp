# Route53 broad-condition fixture: no data sources needed. The condition lint
# should fail because a wildcard hosted-zone grant with
# NormalizedRecordNames=["*"] is equivalent to broad record mutation.
