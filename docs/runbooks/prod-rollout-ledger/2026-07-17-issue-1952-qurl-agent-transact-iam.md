# qURL agent transaction IAM staging grant (#1952)

- **Owner:** platform team
- **Tracking:** [nhp#1952](https://github.com/layervai/nhp/issues/1952), [nhp#3279](https://github.com/layervai/nhp/issues/3279), [qurl-service#1036](https://github.com/layervai/qurl-service/issues/1036), [qurl-service#1037](https://github.com/layervai/qurl-service/pull/1037)

- [ ] Pre-rollout: merge this PR, then dispatch `qURL Agent Transaction IAM (prod)` from `main` with confirmation `qurl-agent-keys-transact-write`. Review the saved plan before approving the protected `production` apply job. It must contain exactly one create: `aws_iam_role_policy.qurl_agent_keys_transact`. Approve within the enforced two-day window from plan creation. The artifact is retained for three days only to make boundary-time expiry fail clearly; if the approval window expires, re-dispatch and review a fresh plan rather than re-uploading or reusing the stale plan. A pending approval holds the shared `promote-to-prod` concurrency slot and queues regular production promotions, so approve promptly or cancel and re-dispatch when ready.
- [ ] Rollout: let the workflow apply only its saved plan. This is the first end-to-end exercise of the production session's state-lock, KMS-write, state-write, and `PutRolePolicy` permissions. Preserve its sanitized plan, live policy hash, staging-state inventory, and converged no-op plan. If any write is denied, or apply succeeds but post-apply convergence fails, assume the policy or state may already have changed: do not retry apply or re-dispatch the create-only workflow. Stop, inspect the job log and live IAM/staging-state ownership read-only, run a separately reviewed read-only convergence plan, and record the evidence on nhp#1952 before continuing. If the exact session policy needs another action, change it only in a new reviewed PR and review a fresh saved plan. The workflow does **not** deploy qurl-service and does **not** prove effective authorization.
- [ ] HARD GATE before any transactional-writer deployment: a privileged operator must run `iam:SimulatePrincipalPolicy` against `arn:aws:iam::235500187906:role/layerv-nhp-prod-cell0-qurl-api-task`. `dynamodb:TransactWriteItems` on `arn:aws:dynamodb:us-east-2:235500187906:table/layerv-nhp-prod-cell0-qurl-agent-keys` must be `allowed`; the same action on that table's `/index/*`, `...:table/layerv-nhp-prod-cell0-qurl-resources`, and one additional non-target qURL table must be denied. Record sanitized inputs/results, Organizations/SCP decision detail, task-role ARN, live policy hash, UTC timestamp, and staging workflow run link on nhp#1952. A checkbox without linked evidence does not satisfy this gate. The current role has no permissions boundary and the table has no resource policy; re-check both facts with the simulation evidence.
- [ ] After the hard gate: deploy the qurl-service transactional writer through its own reviewed workflow. Do not backfill rows, flip the NHP reader, enable TTL, deploy a new NHP server, or apply the legacy root in the staging-IAM workflow.
- [ ] Observe agent registration/bootstrap success and `TransactWriteItems` denial/error metrics before progressing to the schema-v1 reader/backfill/TTL sequence tracked separately.
- [ ] Rollback before writer deployment: open a reviewed PR that removes `prevent_destroy`, makes preflight require the exact live policy and staging-state owner, adds `iam:DeleteRolePolicy` only for the exact role to the protected apply session, and changes the staging workflow contract to accept exactly one delete. Apply only that saved delete plan. Never use `terraform destroy` or direct IAM mutation.
- [ ] Rollback after writer deployment: first roll qurl-service back to the pre-transaction writer and prove bootstrap succeeds; only then use the reviewed exact-delete staging plan above.
- [ ] Permanent handoff, only after nhp#3279 restores an independently acceptable legacy-root plan: add an identical standalone `aws_iam_role_policy.task_agent_keys_transact` to the qurl-service module (do not fold the action into `task_dynamodb`), disable the staging create workflow, and block normal main-root apply during transfer. Back up both state versions and verify the live document hash. Through a separately reviewed, production-approved handoff job under the same `promote-to-prod` concurrency lock, remove the staging address from state **without destroying AWS**, then immediately import `layerv-nhp-prod-cell0-qurl-api-task:qurl-agent-keys-transact-write` at `module.nhp.module.qurl_service[0].aws_iam_role_policy.task_agent_keys_transact`. Verify staging no longer owns it, main state does, and the live hash is unchanged. If import fails, immediately re-import into staging while its source remains present; do not apply either root. No local state mutation is authorized.
- [ ] Cleanup: only after successful state transfer, remove this staged root/workflow in a separate PR and leave the versioned empty state object as audit evidence. Clear the main-plan handoff guard only after a reviewed full plan. Delete this ledger entry when every task is complete.

Read-only proof on 2026-07-17 used checksum-verified Terraform 1.14.3 against the exact production state with `-lock=false`. Targeting only the legacy qURL task DynamoDB policy failed because seven unrelated moved addresses were excluded. Adding only Terraform's required move targets expanded to `Plan: 10 to add, 14 to change, 2 to destroy`, including premature table TTL, NHP reader IAM, new tables, compute/fleet, bootstrap-ALB, secret, and apply-role changes. No plan was applied and no workflow, state, IAM, table, or service was mutated.
