# 2026-07-20 · Connector Authority publisher IAM

- **Owner:** prod rollout coordinator
- **Source:** [nhp#3278](https://github.com/layervai/nhp/issues/3278) · paired qurl-service Connector Authority image publication

Apply the dedicated qurl-service publisher identity before any authority image
publication. This creates no Lambda, Hub listener, DNS, public HTTP surface, or
runtime activation.

- [ ] Pre-rollout: after the reusable `Control Sandbox Update` workflow is merged, dispatch its separate `plan` and `apply` operations from the same live `main` commit. Review the complete plan artifact and copy every state/plan selector from its summary; the apply must consume that single-use saved plan, reject reruns, and stop on any main or state movement. Require all pre-existing 41 resources to be no-op and allow only exact create/partial-retry of `layerv-nhp-<env>-control-connector-authority-publisher` plus its `publish-connector-authority` inline policy. Before production, converge the shared apply role to reviewed source and re-simulate its exact role/policy lifecycle actions. _Read-only 2026-07-20 evidence: both accounts have the exact `token.actions.githubusercontent.com` OIDC provider; sandbox allowed every simulated action, while prod allowed every create-path action but still implicitly denied `iam:UpdateRoleDescription` because its live shared policy predates the source grant tracked by nhp#3278. Do not bypass that prerequisite with a local apply._ _Sandbox apply evidence: [plan run 29792261229](https://github.com/layervai/nhp/actions/runs/29792261229) at commit `94b2d0f53d63e9e3a9ba14a03a6cdb7ba60d3c56` admitted exactly two creates and 41 no-ops; its single-use saved plan had SHA-256 `abe151635d4d8ab8ef662eee7e67b15bb5beba567e270cf7fb5beef9bc3c345c` against state serial 4 / VersionId `GVKRXxNVgZG0L6Cr11dCc3UOrYDPLR0A`. [Apply run 29792335495](https://github.com/layervai/nhp/actions/runs/29792335495) created only the reviewed role and policy and advanced state to serial 5 / VersionId `KZ1qIFhec_xU4MCnJXdm2dNOkFBfrGDE`. The post-apply refresh found only the AWS provider's computed reflection of that separately managed policy onto the role's `inline_policy` state. Merge the source-owned exact normalization contract, run its separate `plan-refresh-only` and saved-plan `apply`, and then require a normal refresh-enabled exact 43-resource no-op. The normalization plan must be state-only: no create, update, or delete of an AWS object._ Apply sandbox and record that final no-op before production.
- [ ] Rollout: apply the production Control plan only after sandbox proof. Record the role trust and policy JSON: qurl-service GitHub Environment `sandbox` in sandbox and `production` in prod; only `ecr:GetAuthorizationToken` has `Resource: *`; every other ECR action targets `layerv/qurl-connector-authority`, and `ssm:{Get,Put}Parameter` targets only `/<env>/nhp/control/connector-authority/image-digest`.
- [ ] Cross-repo: before enabling publication, confirm the qurl-service `sandbox` and `production` GitHub Environments allow deployments only from `main`. Keep `sandbox` reviewer-free: it is the shared continuous-deploy lane, so normal PR merge review is its human gate and adding an Environment reviewer would also block unrelated sandbox releases. Require a designated reviewer and production-appropriate approval policy on `production`. Then configure each Environment with the corresponding `authority_publisher_role_arn` output. Before its first write, require the publisher workflow to verify the pushed manifest in ECR, derive and validate canonical lowercase `sha256:<64hex>`, write that digest to the exact SSM parameter, read it back exactly, and retain publication evidence. Do not publish or update the pin through the shared NHP Terraform apply role.
- [ ] Post-rollout: require refresh-enabled 43-resource Control no-op plans in sandbox and production, and verify CloudTrail shows only the expected qurl-service Environment session using the publisher role for exact ECR/SSM targets. Keep every authority Lambda/runtime deployment blocked until the immutable digest pin and later runtime plan are separately reviewed.
- [ ] Rollback: disable the qurl-service publisher workflow/environment role reference first. Leave the immutable ECR repository and last known-good digest pin intact for audit/rollback; revert and apply the publisher IAM resources only after proving no publication session depends on them. Never replace the digest with `UNPUBLISHED` after a valid image has shipped.
