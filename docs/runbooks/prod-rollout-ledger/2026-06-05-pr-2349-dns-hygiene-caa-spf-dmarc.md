# 2026-06-05 · PR #2349 · DNS hygiene: CAA + SPF/DMARC across apex domains

- **Owner:** prod rollout coordinator
- **Source:** [#2349](https://github.com/layervai/nhp/pull/2349) · [issue #1149](https://github.com/layervai/nhp/issues/1149)

Adds CAA + SPF/DMARC Route53 records for layerv.ai / qurl.site / qurl.link (prod) and layerv.xyz (sandbox). Prod qurl.site / qurl.link / layerv.ai DNS hygiene records are not yet live; layerv.xyz sandbox DNS hygiene records are already live and match Terraform as of the 2026-06-18 pre-rollout re-check. Status: Ready for the remaining prod rollout; pre-rollout hard gates re-verified below.

- [x] Pre-rollout (HARD): CAA-completeness re-check — re-run the crt.sh issuer scan in the PR for layerv.ai / layerv.xyz / qurl.link / qurl.site and add any new CA's CAA identifier in `terraform/modules/dns-hygiene-constants` (omitting an active CA silently breaks its next renewal). COMPLETED 2026-06-18 with:
  - `curl --compressed -fsSL --retry 5 --retry-delay 2 --retry-all-errors --max-time 120 "https://crt.sh/?q=%25.<domain>&output=json&exclude=expired" | jq ...`
  - `curl --compressed -fsSL --retry 5 --retry-delay 2 --retry-all-errors --max-time 120 "https://crt.sh/?q=<domain>&output=json&exclude=expired" | jq ...`
  - crt.sh returned one transient HTTP 502 on qurl.link during the first pass, then succeeded under the same retrying command.
  - Active issuers: layerv.ai = Amazon RSA 2048 M01/M04, Let's Encrypt E7/E8/R12/R13/YE2/YR1/YR2, Go Daddy Secure Certificate Authority - G2; layerv.xyz = Amazon RSA 2048 M01/M04, Let's Encrypt E7/E8/R12/R13/YE1/YE2/YR1, Go Daddy Secure Certificate Authority - G2; qurl.link = Amazon RSA 2048 M01/M04 + Let's Encrypt R12/YR2; qurl.site = Let's Encrypt R12/YR2.
  - Outcome: existing `terraform/modules/dns-hygiene-constants` CAA families cover the active issuer set (`caa_acm`, `caa_letsencrypt`, `caa_godaddy`, `caa_iodef`). No CAA constant patch required.
- [x] Pre-rollout (HARD): apex-TXT single-set clobber check — `dig +short TXT layerv.xyz` / `_dmarc.layerv.xyz` (overwrite=true; fold any new `google-site-verification`/`MS=` value into `sandbox/dns_hygiene.tf`) and `dig +short TXT qurl.link qurl.site` / `_dmarc.*` (overwrite=false; an out-of-band TXT fails the apply loud — fold into `prod/dns_hygiene.tf` first). Also confirm `security@layerv.ai` (CAA iodef) is a monitored inbox before CAs deliver policy reports. COMPLETED 2026-06-18 with:
  - `dig +short TXT layerv.xyz` -> `"v=spf1 include:spf.improvmx.com include:amazonses.com -all"`
  - `dig +short TXT _dmarc.layerv.xyz` -> `"v=DMARC1; p=reject; sp=reject; rua=mailto:dmarc@layerv.ai; pct=100"`
  - `dig +short TXT qurl.link`, `dig +short TXT _dmarc.qurl.link`, `dig +short TXT qurl.site`, and `dig +short TXT _dmarc.qurl.site` -> no records.
  - `AWS_PROFILE=layerv aws route53 list-resource-record-sets --hosted-zone-id Z10394893FM38A1RXLL32 ...` confirmed layerv.xyz CAA/SPF/DMARC are already live and match `terraform/environments/sandbox/dns_hygiene.tf`.
  - `AWS_PROFILE=layerv-mgmt aws route53 list-resource-record-sets --hosted-zone-id Z0693053DKJ8S3XN9WPG ...` and `... Z06942509AYXSB91X7CD ...` returned no TXT/CAA/MX/DKIM hygiene records for qurl.link/qurl.site, matching the prod create-only posture.
  - `AWS_PROFILE=layerv aws sns list-subscriptions --region us-east-2 ...` confirmed non-pending email subscriptions for `security@layerv.ai` on `layerv-nhp-sandbox-guardduty-email` and `layerv-nhp-sandbox-acme-cert-alerts`. Slack search for `"security@layerv.ai"` found the 2025-12-11 `#website-dev` note that the security alias was set up to route to Justin's email ("Aliasing is free. All done") and the 2026-03-11 `#all-layerv` public security-reporting guidance. This confirms a routed, monitored intake for CAA iodef purposes; no evidence was found for a dedicated staffed queue beyond that alias/alert path.
  - Outcome: no out-of-band apex TXT values need folding into Terraform.
- [x] Pre-rollout (HARD): confirm no `*.layerv.xyz` subdomain sends mail under its own envelope domain (DMARC `p=reject` + `sp=reject` inherit to all subdomains); give any sender an aligned SPF/DKIM + its own `_dmarc`, or scope DMARC down, before flipping. COMPLETED 2026-06-18 with caveat:
  - `AWS_PROFILE=layerv aws route53 list-resource-record-sets --hosted-zone-id Z10394893FM38A1RXLL32 ...` found `ses.layerv.xyz` MX `10 feedback-smtp.us-east-2.amazonses.com` and TXT `"v=spf1 include:amazonses.com ~all"`, plus SES DKIM CNAMEs under `_domainkey.layerv.xyz`.
  - `AWS_PROFILE=layerv aws sesv2 get-email-identity --region us-east-2 --email-identity layerv.xyz ...` and `--region us-west-2 ...` showed `layerv.xyz` is verified for sending with DKIM `SUCCESS` and custom MAIL FROM `ses.layerv.xyz`. `get-email-identity ses.layerv.xyz` returned `NotFoundException`, so `ses.layerv.xyz` is the envelope MAIL FROM for the apex `layerv.xyz` identity, not a separate SES sending identity.
  - Repo evidence (`rg "email_from_address|noreply@layerv\\.xyz|ses\\.layerv\\.xyz|mail_from|from_email|billing_from_email|amazonses" ...`) found Auth0 sandbox transactional email configured as `LayerV <noreply@layerv.xyz>` and no `*.layerv.xyz` header-from sender. The only subdomain envelope found is the SES MAIL FROM domain above.
  - Outcome: do not state "no subdomain envelope exists"; one does (`ses.layerv.xyz`). It is aligned for apex `layerv.xyz` mail via relaxed SPF and SES DKIM on the apex identity, and no separate `From: *@ses.layerv.xyz` / other `*.layerv.xyz` sender was found. Add a dedicated `_dmarc.<subdomain>` before any future code/config sends with a subdomain RFC5322.From domain.
- [ ] Rollout: sandbox apply (build-and-push) writes layerv.xyz CAA/SPF/DMARC; prod apply (promote, `run_terraform=true`) writes layerv.ai/qurl.site/qurl.link CAA, qurl.* SPF/DMARC, and the three `_report._dmarc` cross-domain auth records (cross-account via `aws.route53_mgmt`). ORDERING: run the prod apply BEFORE (or with) sandbox — layerv.xyz's `_report._dmarc.layerv.ai` auth record lands in prod, so sandbox-first loses DMARC visibility right after flipping a live sender to `p=reject`.
- [ ] Post-rollout: confirm cert renewals still succeed (no `CAA` errors on the `acme-cert` Lambda / ACM `RenewalEligibility`; specifically GoDaddy's `email.layerv.ai` cert ~2026-08/09 — CAA must keep `godaddy.com`/`starfieldtech.com`). Confirm `dig CAA <each apex>`, `dig TXT qurl.link`, `dig TXT _dmarc.qurl.link`, `dig TXT qurl.link._report._dmarc.layerv.ai`. DMARC aggregate reports must arrive at dmarc@layerv.ai for qurl.link/qurl.site/layerv.xyz on day 1 (with `p=reject` a forgotten sender otherwise bounces); watch for legitimate-mail rejections.
- [ ] Rollback: SPF/DMARC — in-place update (layerv.xyz back to `~all`/`p=none` and re-apply; `prevent_destroy` allows updates). CAA — in-place widen by adding the missing CA to `dns-hygiene-constants` and re-applying; full record deletion requires dropping the `prevent_destroy` block, applying, then deleting the resource.
- [ ] Follow-ups: [#1149](https://github.com/layervai/nhp/issues/1149) stays open (DNSSEC parity + Namecheap registrar locks not addressed); raise `dns_hygiene_ttl` from 300s to ~3600 after bake-in; null-MX (RFC 7505) on non-sending qurl domains (out of scope here).
