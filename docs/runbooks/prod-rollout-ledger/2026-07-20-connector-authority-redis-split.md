# 2026-07-20 · Connector Authority Redis role split

- **Owner:** prod rollout coordinator
- **Source:** [nhp#3278](https://github.com/layervai/nhp/issues/3278) · [nhp#3279](https://github.com/layervai/nhp/issues/3279) · [nhp#3362](https://github.com/layervai/nhp/issues/3362) · [qurl-service#1302](https://github.com/layervai/qurl-service/pull/1302)

Apply the least-privilege issuer/activator Redis users only after the publisher
foundation is independently converged. Do not combine the two reviewed Control
transitions or enable either runtime against the detached legacy user.

- [x] Pre-rollout: the apply job in failed-safe [run 29792335495](https://github.com/layervai/nhp/actions/runs/29792335495) consumed the saved publisher plan and completed only the 41-to-43 role/policy transition; the overall run later reported red when its strict post-apply normalization proof stopped. State-only normalization apply [run 29875008664](https://github.com/layervai/nhp/actions/runs/29875008664) then consumed its own saved artifact and recorded the refresh-enabled exact 43-resource sandbox no-op on current `main`. Require the subsequent Redis plan to contain only creation of either or both split users plus the exact user-group membership replacement; never apply a combined publisher-and-Redis plan. Track the intentionally deferred legacy-user deletion in nhp#3362.
- [ ] Rollout: apply the exact 43-to-45 Redis transition in sandbox only. Require the user group to contain only the disabled default, issuer, and activator users; keep the legacy `otp-auth` user detached. A trusted qurl-service publication may have advanced the externally owned Connector Authority image-digest SSM parameter since Terraform last wrote state. If so, admit only its exact `value` + monotonic `version` observation alongside the Redis transition, bind its value-free drift hash into the saved-plan contract, and re-prove the identical live observation immediately before apply; the SSM parameter itself must remain a no-op resource action. Record a refresh-enabled exact 45-resource sandbox no-op after apply.
- [ ] Production: remain blocked while nhp#3279 or any production legacy/state gate is open. Do not reuse the sandbox 43-to-45 transition: production Control currently has no foundation state. After every gate closes, review a separate production first-apply/current-main contract for the then-current complete foundation (expected to be 45 resources if this inventory is unchanged), apply its saved plan through the reviewed workflow, and record a refresh-enabled exact no-op against that same inventory.
- [ ] Cross-repo: qurl-service#1302 already supplies go-redis `ClusterClient`, disables CLIENT SETINFO and maintenance-notification handshakes, disables retries and redirect replay (`MaxRetries: -1`, `MaxRedirects: -1`, effective redirect budget zero), fences ticket JTI key syntax, and pins the command inventory used by these ACLs. Before either runtime connects, its deployment IaC must additionally bind the issuer and activator execution roles only to their corresponding Redis user ARN and live proof must show `COMMAND` and `CLUSTER SLOTS` discovery without `ASKING`; MOVED, ASK, and network failures must fail the current mutation closed rather than replay it implicitly.
- [ ] Post-rollout: verify Redis OSS major version 7, IAM-only zero-password users, exact read/write key patterns, exact command allow lists, and no `elasticache:Connect` grant to the legacy user. Exercise issuance, rate limiting, mismatch/lockout, successful consumption, reissue supersession, and retry behavior; retain logs showing no ACL denials or unexpected CLIENT commands. Complete nhp#3362 only after both runtimes are live-proven on the split users, then delete this ledger entry after recording there a reviewed cleanup plan that deletes exactly the detached legacy `otp-auth` Redis user while every other address in the then-current Control inventory is no-op, followed by a refresh-enabled no-op at the resulting then-current inventory.
- [ ] Rollback: disable issuer and activator deployments and their `elasticache:Connect` grants before reverting the user-group membership. Because this path is greenfield, do not grant a runtime access to the broad legacy user as a rollback shortcut; leave the cache dark until the reviewed split configuration is restored.
