# 2026-06-26 · PR #2808 · qURL v2 revocation-latency SLO + alarms

- **Owner:** prod rollout coordinator
- **Source:** [#2808](https://github.com/layervai/nhp/pull/2808) · gate [#2792](https://github.com/layervai/nhp/issues/2792) (+ [#2793](https://github.com/layervai/nhp/issues/2793))

Adds the `RevocationDeliveryLatency` histogram (NHP_REV emit -> AC NHP_RVA ack), a p99 < 15s SLO alarm, a raw `RevocationAgedOut` non-delivery detector, and a `RevocationUntrackable` invariant-break alarm. All metric alarms key on `{Environment, Cell}` and are additive. At the time of PR #2808 the sample/ack data existed only once the revocation retry engine was armed; issue #2793's activation ledger now tracks that fleet flip. After the per-slot proof fix (#2793), histogram samples are per targeted AC slot, not per `acId` or per revoke event; blue/green overlap can increase sample volume/weighting and aged-out counts, so read p99 as the per-slot delivery SLO. Issue #2868 adds `DeploymentWindow` plus the `revocation-aged-out-page` composite so deploy-overlap age-outs do not page during the roughly 10-minute window after the latest deploy heartbeat while the raw detector remains `Sum >= 1`; transient suppressor-window age-outs are not delayed for a later page unless the raw detector remains or repeats after the suppressor clears, and the no-action `revocation-aged-out-suppressed` composite records the raw+window overlap for audit.

- [ ] Rollout: applied by the normal prod promote terraform apply (additive CloudWatch monitoring changes, no replacements/deletions). Verify the plan diff includes the raw metric alarms (`revocation_delivery_latency_high`, `revocation_aged_out`, `revocation_untrackable`), the no-action `revocation_deploy_window` suppressor, and the SNS-backed `revocation_aged_out_page` composite under the prod cell.
- [ ] Post-rollout: confirm the actionable alarms are `OK`/`INSUFFICIENT_DATA` (not erroneously `ALARM`): `<name_prefix>-<cell>-revocation-delivery-latency`, `<name_prefix>-<cell>-revocation-aged-out-page`, and `<name_prefix>-<cell>-revocation-untrackable`. The raw `<name_prefix>-<cell>-revocation-aged-out` alarm, `<name_prefix>-<cell>-revocation-deploy-window` suppressor, and `<name_prefix>-<cell>-revocation-aged-out-suppressed` breadcrumb have no direct actions; inspect them for evidence, not paging state. Before issue #2793's activation flip, no samples yet was expected; after the flip, delivered revokes should emit `RevocationDeliveryLatency` and unproven revokes should emit `RevocationAgedOut`.
- [ ] Post-rollout (SLO DATA PRECONDITION): the histogram, aged-out, and untrackable signals are emitted **only** when `NHP_REVOCATION_RETRY_ENABLED=true` (the per-AC `firstSentAt` exists only with the engine armed). This PR shipped the alarms inert-ready; the activation is tracked in `2026-06-30-issue-2793-revocation-proof-enable.md`.
- [ ] Merge/deploy guard: #2793 also rejects empty/whitespace `ACId` in `HandleACOnline` before registration, and this guard is live on merge (not behind `NHP_REVOCATION_RETRY_ENABLED`). Before deploying this PR, confirm prod and sandbox have no blank AC assignment rows and no recent empty-`ACId` registration logs (`server-ac(#...)[HandleACOnline]`).
- [ ] Alarm triage: raw `RevocationAgedOut` means a targeted AC slot never acked before the retry deadline. The retry engine intentionally does not prune pending entries on disconnect or unknown drop because that is not proof the AC slot no longer hosts the flow. A blue/green or canary deploy can legitimately age out the decommissioned slot once the retry engine is armed; `DeploymentWindow` suppresses the SNS-backed `revocation-aged-out-page` composite during the recent deploy-heartbeat window while the raw signal remains visible and `revocation-aged-out-suppressed` records the raw+window overlap as a no-action breadcrumb. A transient suppressor-window age-out is not queued for a later page unless the raw detector remains or repeats after the suppressor clears. Issue #2974 moves `DeploymentWindow` to the deploy-only `LayerV/NHP/Deploy` namespace, denies server/AC app metric roles from that namespace, and adds `<name_prefix>-<cell>-revocation-deploy-window-without-run` to page if a suppressor datapoint appears without the paired `DeploymentWindowRun` marker. Any `revocation-aged-out-page` notification is a non-deploy immediate-revocation proof failure outside that suppressor. `RevocationUntrackable` means fanout was enqueued but the server could not key the connection to `ACId`/authenticated pubkey; check server logs for `cannot track fanout` and treat it as a live-connection registry invariant break.
- [ ] Lockstep guard: the alarm threshold (15000 ms) mirrors `RevocationDeliveryLatencyP99SLO` (15s) in `endpoints/server/revocation_retry.go`. If either moves, move both in the same PR.
- [ ] Rollback: revert this PR (or remove the three alarm resources from `terraform/modules/monitoring/main.tf`) and apply — removes the alarms only; the Go-side metric recording is harmless to leave (no-op while the engine is OFF).
