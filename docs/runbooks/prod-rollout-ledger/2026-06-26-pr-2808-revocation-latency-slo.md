# 2026-06-26 · PR #2808 · qURL v2 revocation-latency SLO + alarms

- **Owner:** prod rollout coordinator
- **Source:** [#2808](https://github.com/layervai/nhp/pull/2808) · gate [#2792](https://github.com/layervai/nhp/issues/2792) (+ [#2793](https://github.com/layervai/nhp/issues/2793))

Adds the `RevocationDeliveryLatency` histogram (NHP_REV emit -> AC NHP_RACK ack), a p99 < 15s SLO alarm, a companion `RevocationAgedOut` non-delivery alarm, and a `RevocationUntrackable` invariant-break alarm. All alarms key on `{Environment, Cell}` and are additive. Sample/ack data only exists once the revocation retry engine is armed. After the per-slot proof fix (#2793), histogram samples are per targeted AC slot, not per `acId` or per revoke event; blue/green overlap can increase sample volume/weighting and aged-out counts, so read p99 as the per-slot delivery SLO.

- [ ] Rollout: applied by the normal prod promote terraform apply (additive — three new `aws_cloudwatch_metric_alarm` in `modules/monitoring`, no replacements/deletions). Verify the plan diff shows only `revocation_delivery_latency_high`, `revocation_aged_out`, and `revocation_untrackable` creations under the prod cell.
- [ ] Post-rollout: confirm all three alarms exist and are `OK`/`INSUFFICIENT_DATA` (not erroneously `ALARM`): `<name_prefix>-<cell>-revocation-delivery-latency`, `<name_prefix>-<cell>-revocation-aged-out`, and `<name_prefix>-<cell>-revocation-untrackable`. With the retry engine still OFF they should sit `INSUFFICIENT_DATA`/`OK` under `treat_missing_data="notBreaching"` (no samples yet) — that is expected, not a fault.
- [ ] Post-rollout (SLO DATA PRECONDITION): the histogram, aged-out, and untrackable signals are emitted **only** when `NHP_REVOCATION_RETRY_ENABLED=true` (the per-AC `firstSentAt` exists only with the engine armed). Until the fleet ships AC ack support (#2793) and the engine is flipped on, `RevocationDeliveryLatency` collects zero samples and the p99 alarm cannot fire. Arming the engine is gated separately — track there, not here; this entry only covers the alarms shipping inert-ready.
- [ ] Merge/deploy guard: #2793 also rejects empty/whitespace `ACId` in `HandleACOnline` before registration, and this guard is live on merge (not behind `NHP_REVOCATION_RETRY_ENABLED`). Before deploying this PR, confirm prod and sandbox have no blank AC assignment rows and no recent empty-`ACId` registration logs (`server-ac(#...)[HandleACOnline]`).
- [ ] Alarm triage: `RevocationAgedOut` means a targeted AC slot never acked before the retry deadline. A blue/green deploy can legitimately age out the decommissioned slot once the retry engine is armed; correlate with deploy windows before paging it as an AC-delivery outage, while still treating non-deploy age-outs as immediate-revocation proof failures. `RevocationUntrackable` means fanout was enqueued but the server could not key the connection to `ACId`/authenticated pubkey; check server logs for `cannot track fanout` and treat it as a live-connection registry invariant break.
- [ ] Lockstep guard: the alarm threshold (15000 ms) mirrors `RevocationDeliveryLatencyP99SLO` (15s) in `endpoints/server/revocation_retry.go`. If either moves, move both in the same PR.
- [ ] Rollback: revert this PR (or remove the three alarm resources from `terraform/modules/monitoring/main.tf`) and apply — removes the alarms only; the Go-side metric recording is harmless to leave (no-op while the engine is OFF).
