# 2026-06-14 · issue #2490 · qurl-scanner tombstone-write activation (sandbox)

- **Owner:** prod rollout coordinator
- **Source:** [#2490](https://github.com/layervai/nhp/issues/2490), [#2489](https://github.com/layervai/nhp/pull/2489) (sqs-emit activation, prerequisite), [#2487](https://github.com/layervai/nhp/pull/2487) (plumbing — added `qurl_scanner_tombstone_write_enabled`), [#2326](2026-06-05-pr-2326-qurl-scanner-lambda.md) (HARD PROD preconditions still pending), [#2491](https://github.com/layervai/nhp/issues/2491) (SNS alarm wiring, prod prerequisite)

Sandbox-only tombstone-write activation; prod tfvars stay unset (default false). A 2026-06-18 synthetic confirmed the flag and expiry path were live but exposed the missing scanner-role `dynamodb:DeleteItem`; #2716 has landed/applied that IAM grant, leaving the post-apply synthetic proof below.

- [x] Pre-rollout: re-confirm the gate immediately before merge (build-and-push.yml auto-applies `terraform/**` on push to main). `qurl_scanner_sqs_emit_enabled` is live and the emit/consumer burn-in is still clean: `*-qurl-resource-lifecycle-dlq` at depth 0, scanner Lambda `Errors` Sum = 0 over the last 24h, and `NumberOfMessagesSent ≈ NumberOfMessagesDeleted` on `*-qurl-resource-lifecycle`. Verify via `aws sqs get-queue-attributes --profile layerv --region us-east-2 --queue-url <dlq-url> --attribute-names ApproximateNumberOfMessages` + the `AWS/Lambda Errors` / `AWS/SQS` metrics for `layerv-nhp-sandbox-cell0-qurl-scanner` and `layerv-nhp-sandbox-cell0-qurl-resource-lifecycle`. _Done/current 2026-06-17: queue depth and DLQ depth 0; scanner Lambda `Errors` Sum = 0 over the last 24h; SQS sent/deleted both 230._
- [x] Pre-rollout (image-tag identity): confirm tombstone-write activates on the image the burn-in actually ran on, since `image_uri` resolves at plan time from the current SSM scanner tag. _Done 2026-06-17: SQS/Errors burn-in rechecked against SSM tag `cef5f89`. Current 2026-06-18: SSM and Lambda `Code.ImageUri` resolve to `cc42958`; the rollout call-site check below pins the digest/source commit._
- [x] Rollout: post-merge build-and-push run completes its sandbox apply and the scanner Lambda env shows `QURL_SCANNER_ENABLE_TOMBSTONE_WRITE=true`. Verify via `aws lambda get-function-configuration --profile layerv --region us-east-2 --function-name layerv-nhp-sandbox-cell0-qurl-scanner --query 'Environment.Variables'`. _Done 2026-06-17: Lambda env contains `QURL_SCANNER_ENABLE_TOMBSTONE_WRITE=true`._
- [x] Rollout: confirm the #2716 scanner-role IAM follow-up is applied to the live sandbox role. Verify via `aws iam get-role-policy --profile layerv --role-name layerv-nhp-sandbox-cell0-qurl-scanner-execution --policy-name dynamodb-access --query "PolicyDocument.Statement[?Sid=='QurlResourcesReadWrite'].Action[]" --output json`. _Done/current 2026-06-18: live role policy includes `DeleteItem` / `GetItem` / `UpdateItem`; #2716's Terraform Sid is the durable source. Pre-apply failure evidence: synthetic resource `r_zc0tzgecpdc` failed tombstone-write with `AccessDeniedException` for `dynamodb:DeleteItem` on `layerv-nhp-sandbox-cell0-qurl-resources`._
- [x] Rollout: re-check the qurl-service scanner DDB call sites before treating the Terraform action breakdown as canonical. _Done/current 2026-06-18: qurl-service `origin/main` `cc42958fc81f0f77eb9556d50821704541e55ea8` matches live scanner tag `cc42958` at ECR digest `sha256:d9c885c9d0480f1dbc74c648eaed27d1046609e14a2f93efb1347b67d07ead9e`; `TombstoneTransit` still uses consistent `GetItem`, tombstone `UpdateItem` attrs, and sentinel `Delete`._
- [ ] Post-rollout (**the #2490 verification**): mint a sandbox transit resource with a 60s TTL, wait past expiry (+ one scanner tick), then `POST /v1/resources/{id}/qurls` (re-mint) → expect **410 Gone**. Do not delete this ledger until this synthetic, the durable tombstone-field check, and the 24h delete-path burn-in are green.
- [ ] Post-rollout: confirm the durable tombstone landed — the closed resource's item in the `*-qurl-resources` DDB table carries `resource_tombstoned_at` (and `tombstone_ttl`). Establishes the write actually fired, not just that the API refused the re-mint.
- [ ] Post-rollout: after a successful tombstone delete fires, verify scanner Lambda `Errors` stays 0 and the DLQ stays empty for ≥24h. This specifically exercises the newly applied #2716 `DeleteItem` path.
- [ ] Rollback (**not fully reversible**): flip `qurl_scanner_tombstone_write_enabled = false` and re-apply → scanner stops writing NEW tombstones; resources already tombstoned stay 410-Gone (durable). Rollback halts the behavior, it does not undo it.
- [x] Known limitation (carried from #2489, filed as [#2491](https://github.com/layervai/nhp/issues/2491)): sandbox `scanner_lambda_alarm_sns_topic_arn` is empty, so the scanner `Errors` alarm pages no one. The manual `Errors` / DLQ checks above are the safety net until #2491 wires SNS — which must land before the prod tombstone-write flip. _Resolved/current 2026-06-17: scanner `errors-burning` and resource-lifecycle backlog/DLQ alarms have non-empty actions to the cell alerts SNS topic._

**Prod follow-up:** a separate PR, tracked by #2490 / #2491 / the #2326 ledger entry — not a task here. Delete this file once the sandbox checkboxes above are done.
