# 2026-06-06 · PR #2343 · Traefik runs as non-root nhp-traefik user

- **Owner:** prod rollout coordinator
- **Source:** [#2343](https://github.com/layervai/nhp/pull/2343) · [issue #1090](https://github.com/layervai/nhp/issues/1090)

Runs AC Traefik as `nhp-traefik` (with `CAP_NET_BIND_SERVICE`). Not yet in prod: a launch-template bump rolled per env (sandbox first, then prod) via instance refresh; a missed perm is a prod-TLS-outage class failure.

- [ ] Cross-repo (traefik-plugins): `nhp-traefik` only READS SSM-deployed plugin sources under `/home/ubuntu/traefik/plugins-local` — those must stay world-readable (644 files / 755 dirs). Verify a `traefik-plugins` SSM redeploy still loads after rollout; if that repo ever tightens perms it must chown to `nhp-traefik`.
- [ ] Pre-rollout: after the sandbox AC-ASG instance refresh (plan/apply doesn't exercise this), run PR #2343's sandbox functional validation. Highest-signal checks: `systemctl show traefik` reports `User=nhp-traefik` + `CAP_NET_BIND_SERVICE` and the unit is active; HTTPS serves on `:443`; `*-ac-custom-domain-cert-sync` re-syncs a `nhp-traefik`-owned 0600 `privkey.pem` and the custom-domain route serves; outbound TLS works under `ProtectSystem=strict` (ACME/Route53/Secrets Manager + cross-account DNS challenge); Yaegi plugin loading still works with `plugins-local` read-only. Full list in the PR.
- [ ] Rollout: launch-template bump — roll the AC ASG via instance refresh per env (sandbox first, then prod). The `custom-domain-cert-sync.sh` change takes effect at apply (ships in the SSM doc; re-runs on doc change + every 6h) BEFORE refresh — transition-safe via `ubuntu` fallback when `nhp-traefik` is absent; no separate ordering step.
- [ ] Post-rollout: confirm AC target groups stay healthy through the refresh, no TLS/middleware errors in `traefik.log`. Cert-sync now fails LOUD (exit 1, keyed off `/etc/nhp-traefik-nonroot` sentinel) BEFORE the `CertSyncFailures` put-metric-data, so the `cert_sync_failures` alarm does NOT catch it — (a) confirm cert-sync association non-compliance is routed to a human, (b) confirm no refreshed instance logs `FATAL: '...' absent but non-root sentinel present` (dedicated metric/alarm tracked in [#2387](https://github.com/layervai/nhp/issues/2387)). Validate a `traefik-plugins` SSM redeploy's updated code is served after restart (`ReadOnlyPaths` bind mount); sanity-check nothing sensitive is world-readable directly under `/home/ubuntu`.
- [ ] Rollback: revert the PR and instance-refresh the AC ASG to the prior launch-template version (self-contained to AC `user_data` + `custom-domain-cert-sync.sh`).
- [ ] Follow-up: PR 3 (nhp-acd non-root) deferred under [#1090](https://github.com/layervai/nhp/issues/1090) — nhp-acd must keep `CAP_NET_ADMIN`/`CAP_NET_RAW` for iptables.
