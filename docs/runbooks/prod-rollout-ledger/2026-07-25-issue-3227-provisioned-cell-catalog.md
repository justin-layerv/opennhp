# 2026-07-25 · Issue #3227 · Provisioned cell catalog

- **Owner:** prod rollout coordinator
- **Source:** [issue #3227](https://github.com/layervai/nhp/issues/3227)

Seed only the reviewed sandbox Control-owned native-UDP cell catalog. This does
not enable the Authority runtime, Hub, or production catalog.

- [x] Producer provenance: using the configured `layerv` AWS profile in sandbox account `767397897469`, Terraform 1.14.3 readback from `terraform/environments/sandbox-cell1` returned `cell1.nhp.layerv.xyz` and server public key `Sb4lH7rfkKTagGvpKeBx/ArYual9fM4EQCQkiqxGNBs=`. A Secrets Manager readback that emitted only the `publicKey`, `hostname`, and `environment` fields from `layerv-nhp-sandbox-cell1-server` independently matched that key and reported environment `sandbox-cell1`; the equivalent cell0 public-key readback matched `9dVku2oF589tWz9/Hn01STtstgkum4MM4kgKEp7lCw8=`. [PR #3443](https://github.com/layervai/nhp/pull/3443) records the attended cell1 out-of-band apply and converged follow-up plan. No private key material was printed or recorded.
- [x] Catalog ownership: qurl-service commit [`1286e426`](https://github.com/layervai/qurl-service/commit/1286e4263ea4e78b228e277639699a0a05b9b6d3) exposes only `GetItem` and `Query` through the placement repository's DynamoDB client. Registration and recovery transactions bind the exact `REGISTRY/CELL#` row with `ConditionCheck`; they never put, update, or delete that row. Terraform is therefore the sole catalog writer. Runtime code owns assignment, registration, recovery, claim, replay, and counter rows only. Adding any runtime catalog writer requires a separately reviewed ownership redesign before rollout.
- [x] Materialization restored: the temporary Authority-first holdback is complete. Its `authority-image-update` transition was admitted and applied ([PR #3482](https://github.com/layervai/nhp/pull/3482) records the applied basis as the expansion predecessor), so the sandbox `provisioned_cell_catalog_materialization_enabled` default returns to `true` behind a fail-closed `true` validation and the temporary holdback ledger entry is deleted. The reviewed `provisioned_cells` value and the Authority identity contract are unchanged.
- [ ] Rollout ordering: the sandbox Control root has no dispatch input or tfvars for this gate, so the merge of the restoration is itself the gate flip — from that commit on, every Control plan carries the two catalog creates. Merge the restoration only after the in-flight `hub-udp-source-fence-replacement-with-authority-alarm-routing-with-legacy-otp-user-delete` apply has landed and verified, then make the catalog the very next Control apply. A restoration merged ahead of that apply composes the two creates into it, which the trusted checker correctly rejects (the composite falls outside `HUB_SOURCE_FENCE_ACTIONS`) — the same failure the holdback existed to prevent.
- [ ] Rollout: review and apply one saved Terraform 1.14.3 sandbox Control plan whose only non-noop changes are the exact atomic creates of active `REGISTRY/CELL#cell0` and disabled `REGISTRY/CELL#cell1`; the trusted checker must report `plan_mode=provisioned-cell-catalog`. If one independent `PutItem` succeeds before the apply fails, retry the same saved plan so the exact write is idempotently repeated; a newly generated one-row create plan is intentionally rejected and requires attended state recovery if the saved artifact is unavailable.
- [ ] Post-rollout: use strongly consistent reads to prove both rows exactly match the checked-in AttributeValue contract, including opaque LayerV-owned UDP host/port and server public key, prove cell1 is not selectable while disabled, and prove the `REGISTRY/CELL#` partition contains no additional rows.
- [ ] Mutation rule: treat each checked-in `updated_at` as the deterministic row-mutation revision used by qurl-service's optimistic cell fence. Change it with every reviewed mutation to that row; never use Terraform `timestamp()` or apply-time wall clock.
- [ ] Lockstep edit rule: every future row mutation must update the sandbox Control default and its exact-match validation literal, the module Terraform fixtures, and the independent trusted-checker catalog constants in one PR. The authenticated Control plan lane must still accept the complete resulting row set; a partial edit fails closed.
- [x] Cross-repo cell1 activation: DONE 2026-07-27 in an attended review. The original criterion was CIRCULAR and could never be satisfied as written: it required "two-cell sandbox readiness evidence proves assignment, authenticated NHP registration, refresh, and reassignment" BEFORE activation, but that evidence is produced only by the attended two-cell proof, which requires `deployment_producer_run_id`; the manifest producer refuses to emit one while `cell1.status != "active"` (`collect_udp_proof_deployment_evidence.py`), and the governed Control checker pins cell1 `disabled` and rejects any plan that flips it. Written as a precondition, satisfiable only as a post-condition.
  Resolved by SPLITTING it. cell1 is activated on DEPLOYMENT readiness, all four parts verified against live sandbox state on 2026-07-27: (1) [NHP #3454](https://github.com/layervai/nhp/pull/3454) merged, the 11-function two-cell Authority graph authorized; (2) the CIDR collision is resolved — cell1 sits at `10.104.0.0/16`, Control at `10.102.0.0/16`, no overlap; (3) cell1 UDP source-fence convergence APPLIED — `layerv-nhp-sandbox-cell1-edge` active with one security group, ingress exactly `3.141.109.76/32` on UDP 62206, `cell1.nhp.layerv.xyz` repointed in the same transaction; (4) cell1's `server_public_key_b64` matches Secrets Manager byte-for-byte and is unrotated, the instance is healthy in its NLB target group, and runtime attestations now publish (the collector had failed every 5 minutes since deployment — fixed in #3538).
  PROTOCOL readiness — assignment, authenticated NHP registration, refresh, reassignment — is now a POST-activation record, established by the two-cell proof this activation unblocks. If that proof fails, revert this row to `disabled` by the same three-site lockstep edit and re-run the Control plan/apply; nothing else depends on cell1 being assignable.
- [ ] Production: keep `provisioned_cells={}` and apply no catalog rows until a separately reviewed production cell inventory and rollout exists.
- [ ] Rollback: do not delete a catalog row that may have durable assignments. Mark it draining or disabled, migrate assignments, and remove it only through a separately reviewed state operation after readback proves no dependency remains.
