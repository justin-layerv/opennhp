# 2026-06-30 · PR #2753 · qURL v2 keyed-identity epic

- **Owner:** prod rollout coordinator
- **Source:** nhp#2753, qurl-service#994; design `docs/design/QURL_V2_KEYED_IDENTITY.md`

Merges the qURL v2 keyed-identity stack to `main` **dormant**: the AC eBPF
datapath ships with `FilterMode=IPTABLES` (the committed XDP object is present
but not loaded) and v2 issuance/admission is gated off, so the post-merge deploy
is additive and behaviorless. Legacy `at_`/`r_`/`q_` admission is unchanged.

- [ ] Post-rollout: confirm `FilterMode` stays `IPTABLES` (eBPF/XDP not engaged) and v2 admission stays gated after the deploy that ships this merge.
- [ ] Rollback: revert the merge — nothing dormant carries a schema, table, or flag migration, so revert is clean.

The eBPF `FilterMode` flip (E5) and v2-issuance enablement are separate, gated
rollouts that will carry their own ledger entries; they are intentionally **not**
listed here.
