# 2026-06-04 · PR #2318 · qURL internal knock placement hotfix

- **Owner:** prod rollout coordinator
- **Source:** [#2318](https://github.com/layervai/nhp/pull/2318) · [layervai/qurl-service#821](https://github.com/layervai/qurl-service/pull/821) · [layervai/qurl-service#827](https://github.com/layervai/qurl-service/pull/827) · [#2322](https://github.com/layervai/nhp/issues/2322)

NHP server hotfix so route-less internal knocks resolve dynamic `q_` resource rows via direct exact-resource lookup. Not yet in prod: deploy this NHP server fix before any qurl-service prod rollout depending on #821/#827.

- [x] Pre-rollout: confirm the qurl-service build being smoked preserves #821's headless contract (`PublicResolveHandler` derives `srcIP` from `c.ClientIP()`; empty `srcIp` fails closed as `InternalKnockResourceNotFound`) and that dynamic `q_` rows resolve via direct exact-resource lookup with elapsed app-level `ttl` rejected before the DynamoDB TTL sweeper runs. _Repo-side contract verified 2026-06-18: qurl-service `origin/main` `internal/api/handlers/public_resolve.go` derives `srcIP := c.ClientIP()` and passes that into `TriggerKnock`; NHP `resolveInternalKnockResource` fails closed on empty `SrcIp` for dynamic `q_` rows and increments `InternalKnockResourceNotFound`; NHP `ResolveInternalKnockResource` uses direct `LookupResource`; `ResourceLookup.LookupResource` performs a strongly consistent exact `GetItem` against the resource-id-derived dynamic shard and rejects missing/expired direct-row `ttl` before DynamoDB TTL deletion._
- [x] Pre-rollout: confirm `KnockSrcIP` validation compares the same canonical IP shape qurl-service sends; confirm Terraform passes `local.nhp_qurl_dynamic_customer_id_prefix` as the qurl-service module's `nhp_resources_customer_id_prefix` (IAM `dynamodb:LeadingKeys` admits only `<prefix>-??` dynamic shards); confirm writer and reader share the same regional `nhp_resources` replica (`ConsistentRead`, no cross-region RAW). _Repo-side contract verified 2026-06-18: NHP trims and stores `req.SrcIp` after qurl-service supplies `c.ClientIP()`; qurl-service `internal/nhp/knock_client.go` sends that same value as request `SrcIP`; root Terraform passes `local.nhp_qurl_dynamic_customer_id_prefix` into `module.qurl_service.nhp_resources_customer_id_prefix`; the qurl-service IAM policy scopes `dynamodb:LeadingKeys` to `${var.nhp_resources_customer_id_prefix}-??`; NHP and qurl-service share the same `module.dynamodb.resources_table_*` wiring in the environment, and NHP direct lookup uses `ConsistentRead=true`._
- [ ] Pre-rollout (**blocking prod data coverage**): confirm prod has dynamic `q_` catalog rows before any qurl-service rollout that depends on catalog-only routing. _Current 2026-06-18: this is **not satisfied**. qurl-service #827 is closed unmerged and superseded by #855 (merged 2026-06-05), but prod qurl-service is still SSM-pinned to `db5d703` from 2026-06-02. Prod `layerv-nhp-prod-cell0-resources` has `Count=0` rows whose `resource_id` begins with `q_`, while prod `qurl-resources` has 772 active non-expired resources. Keep the qURL producer/cutover blocked until #855 has been live past max qURL lifetime or a backfill populates the dynamic catalog rows._
- [ ] Rollout: deploy this NHP server hotfix to sandbox before re-running the #821 smoke and validating #827 publishing; promote to prod before any qurl-service prod rollout depending on #821/#827.
- [ ] Post-rollout: re-run #821 smoke (headless `/v1/resolve` succeeds, no `knock_failed`) and #827 smoke (fresh dynamic `q_` resolves with no catalog-cache delay); confirm no sustained `InternalKnockResourceNotFound` / strict-auth failures and no qurl-service `NHP resource catalog publish failed` / `nhp_resources` PutItem errors.
- [ ] Post-rollout: confirm `ResourceLookupExpiredDirectRow` near zero (sustained multi-row rate = qurl-service leaking expired rows), `ResourceLookupMissingDirectTTL` zero (nonzero = malformed dynamic rows failing closed); confirm direct `q_` lookups read the resource_id-derived shard only; watch `ac_id-index` read/write capacity during burn-in and per-AZ tunnel-server selection concentration.
- [ ] Rollback: roll back this NHP server image if headless resolves still fail post-hotfix. If failures show `ResourceLookupDirectAspMismatch`/`ResourceLookupMissingDirectTTL`, roll back the qurl-service writer and inspect the affected shard before re-enabling catalog writes.
- [ ] Follow-ups: [#2319](https://github.com/layervai/nhp/issues/2319) (short-TTL cache for dynamic `q_` lookups after QPS measured); [#2322](https://github.com/layervai/nhp/issues/2322) resolved once #2318 + qurl-service#827 merge.
