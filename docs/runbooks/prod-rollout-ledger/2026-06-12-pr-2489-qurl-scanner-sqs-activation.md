# 2026-06-12 · PR #2489 · qurl-scanner SQS data-path activation

- **Owner:** prod rollout coordinator
- **Source:** [#2489](https://github.com/layervai/nhp/pull/2489), [#2471](https://github.com/layervai/nhp/pull/2471) (plumbing), [#2470](https://github.com/layervai/nhp/pull/2470) (ledger ticks), [#2326](docs/runbooks/prod-rollout-ledger/2026-06-05-pr-2326-qurl-scanner-lambda.md) (HARD PROD preconditions still pending)

Sandbox-only activation. Producer (scanner Lambda `EMIT_MODE=sqs`) + consumer (qurl-api `WEBHOOK_EVENTS_CONSUMER_ENABLED=true`) both flip in the same apply triggered by `build-and-push.yml` paths-filtered to `terraform/**` on push to main. Prod tfvars deliberately stay at `false`.

**Resolved by #2491:** the root module now wires `scanner_lambda_alarm_sns_topic_arn` to the cell-wide alerts topic (`module.monitoring.sns_topic_arn`) for both envs, so `resource_lifecycle_queue_backlog` (`threshold = 1000` visible, breaches only when the consumer wedges past the buffer) and `resource_lifecycle_queue_dlq_messages` (`threshold = 0`, any DLQ message) page `#alerts-sandbox` on breach instead of `alarm_actions = []`. The manual "watch `ApproximateNumberOfMessages`" safety net below is retired once #2491 applies. Prod uses the same root wiring and stays inert until the prod scanner flag flips. Current name since #3038: `qurl_service_alarm_sns_topic_arn`.

- [x] Pre-rollout: confirm `/layerv-nhp-sandbox/qurl-api-image-tag` still carries `7791fcc` at merge time (consumer code lives at that SHA). The gating evidence in this PR was captured at 2026-06-12T01:55Z; if the PR sits more than a few hours pre-merge, **re-run this check immediately before clicking merge** because build-and-push.yml auto-applies on push to main and a stale SHA pin would break the consumer roll. Verify via `aws ssm get-parameter --profile layerv --region us-east-2 --name /layerv-nhp-sandbox/qurl-api-image-tag`. _Superseded/current 2026-06-17: SSM now points at newer tag `cef5f89` (2026-06-16T08:27:21Z); the deployed consumer env and burn-in checks below prove the current tag, not stale `7791fcc` evidence._
- [x] Rollout: post-merge build-and-push run completes its `Deploy Sandbox - QURL Service` step with the consumer env vars applied. Verify via `aws ecs describe-task-definition --task-definition layerv-nhp-sandbox-cell0-qurl-api --query 'taskDefinition.containerDefinitions[0].environment[?name==\`WEBHOOK_EVENTS_CONSUMER_ENABLED\`]'`. _Done 2026-06-17: sandbox task definition has `WEBHOOK_EVENTS_CONSUMER_ENABLED=true` and `WEBHOOK_EVENTS_SQS_QUEUE_URL=https://sqs.us-east-2.amazonaws.com/767397897469/layerv-nhp-sandbox-cell0-qurl-resource-lifecycle`._
- [x] Rollout: scanner Lambda env shows `QURL_SCANNER_EMIT_MODE=sqs` + `QURL_SCANNER_SQS_QUEUE_URL=<resource_lifecycle_queue.url>`. Verify via `aws lambda get-function-configuration --function-name layerv-nhp-sandbox-cell0-qurl-scanner --query 'Environment.Variables'`. _Done 2026-06-17: Lambda env has `QURL_SCANNER_EMIT_MODE=sqs`, the resource-lifecycle queue URL, and `QURL_SCANNER_ENABLE_TOMBSTONE_WRITE=true`._
- [x] Post-rollout: first scanner tick after activation emits at least one event when expired qurls are present. Check the `${prefix}-${cell}-qurl-resource-lifecycle` SQS queue `NumberOfMessagesSent` metric in CloudWatch (1-minute granularity). _Done 2026-06-17: last-24h `NumberOfMessagesSent` Sum = 230 on `layerv-nhp-sandbox-cell0-qurl-resource-lifecycle`._
- [x] Post-rollout: qurl-api consumer drains the resource-lifecycle queue. Check `ApproximateNumberOfMessages` stays bounded and `NumberOfMessagesDeleted` matches Sent. _Done 2026-06-17: queue depth 0; last-24h `NumberOfMessagesDeleted` Sum = 230, matching sent._
- [x] Post-rollout: DLQ stays empty. Check `ApproximateNumberOfMessages` on the `*-qurl-resource-lifecycle-dlq` queue is 0. _Done 2026-06-17: DLQ approximate visible/not-visible/delayed counts are 0._
- [x] Post-rollout: burn-in safety net — **keep** the manual 30-min queue-depth watch on `${prefix}-${cell}-qurl-resource-lifecycle` until #2491's post-merge `build-and-push` apply lands and `resource-lifecycle-queue-backlog` + `resource-lifecycle-dlq-messages` are wired to `#alerts-sandbox` (confirm a test notification reaches the channel); **then** retire the manual watch. A backlog > 1000 without a corresponding consumer-roll completion still means a wedged consumer; flip the rollback line below. _Done 2026-06-17: backlog and DLQ alarms both `OK` with alarm/OK actions set to `arn:aws:sns:us-east-2:767397897469:layerv-nhp-sandbox-cell0-alerts`._
- [ ] Post-rollout: e2e — synthetic mint of a transit qurl with 60s TTL, then verify `qurl.expired` + `resource.closed` webhooks land at the Discord bot's `/webhooks/qurl` route AND the qurl-s3-connector's cleanup receiver. Logs at `/aws/ecs/qurl-bot-discord` + connector access logs.
- [ ] Post-rollout: Discord bot DM tense-flip — real `/qurl send` in sandbox with short TTL, observe the DM headline flips from "Closes in N" → "Closed N ago" within ~2 min of expiry.
- [ ] Post-rollout: connector S3 delete — verify the underlying upload is gone from `qurl-s3-connector-uploads-sandbox` after the transit resource closes and refcount reaches 0. (Tombstoning in qurl-service's own DDB is gated separately on #2490; the connector cleanup path runs regardless.)
- [ ] Rollback: flip `qurl_scanner_sqs_emit_enabled = false` in `terraform/environments/sandbox/terraform.tfvars` and re-apply. Producer goes log-only; consumer task def reverts on next ECS roll. Main resource-lifecycle queue retains buffered messages until its 4-day `message_retention_seconds` reaps them (14d on the DLQ if the consumer first fails 3× redeliveries).
- [ ] Cross-repo: bot consumer of `qurl.expired` ships in qurl-integrations (PR-C, task #71 ✓). Connector consumer of `resource.closed` ships in qurl-integrations-infra qurl-s3-connector (PR-B chain, tasks #88/#89/#90 ✓). Both already deployed; activation just turns on the upstream signal.
