# 2026-07-16 · Issue #3227 · Connector Authority foundation

- **Owner:** prod rollout coordinator
- **Source:** [nhp#3227](https://github.com/layervai/nhp/issues/3227), [SES transfer nhp#3273](https://github.com/layervai/nhp/issues/3273)

Create the dark, environment-global Control state before any Hub or authority
runtime is published. Keep it isolated and fail closed throughout rollout.

- [ ] Pre-rollout routing: for sandbox, the `Control Sandbox First Apply` plan and apply dispatches both run `scripts/check-control-global-routing.sh` against `10.102.0.0/16`. It enumerates every enabled region, invokes the regional overlap checker in each, and separately rejects nondeleted Direct Connect gateways, associations, or pending proposals. Stop rather than bypass this check if any routing surface cannot be proven empty. Plan run `29658285646` failed closed before Terraform preflight/init/plan because the apply role lacked `directconnect:DescribeConnections`; apply and verify were skipped, and the run produced no artifact or mutation. #3336 added the exact five-action `DirectConnectRead` expansion to the existing sandbox `terraform_read` policy, and normal main run `29664524687` deployed it in place. Plan run `29664879328` subsequently passed all 17 enabled-region routing audits plus the global Direct Connect gateway inventory. The association and proposal reads are fail-closed source-covered but correctly conditional on a nondeleted gateway; the live audit found none and therefore did not invoke those two APIs. Do not rerun or apply from either failed plan run. Production `10.202.0.0/16` requires an equivalent separately reviewed production workflow; the sandbox workflow cannot target production.
- [ ] Pre-rollout PrivateLink: immediately before each environment's first apply, query EC2 endpoint-service discovery in that target account and region and verify `com.amazonaws.<region>.email` exists as an Interface service in every selected Availability Zone. Stop if it is absent; never substitute the distinct `email-smtp` service.
- [ ] Pre-rollout IAM: explicitly accept the shared `nhp-<env>-github-actions` apply role's ElastiCache user/user-group lifecycle and tag actions, then simulate them against the planned `layerv-nhp-<env>-control-*` user and user-group ARNs. Against cell-scoped ARNs, require every write to remain `implicitDeny` and separately require the intentionally account-wide `Describe*`/`List*` Terraform reads to remain `allowed`; do not conflate read visibility with write confinement. Recheck managed-policy attachments: the 2026-07-16 live count was sandbox 10/10 and production 8/10, while the static worst-case is 10/10. The foundation grant updates the existing `terraform_apply_data` policy and the Direct Connect audit and self-simulation corrections update the existing `terraform_read` policy; neither adds an attachment. Plan run `29664879328` failed closed before endpoint/KMS/state preflight, Terraform init, or plan because the role could not call `iam:SimulatePrincipalPolicy`; #3340 deployed the exact self-role-scoped correction through the normal sandbox writer. Fresh plan run `29667228195` then passed the complete routing audit and failed closed in IAM preflight: the checker incorrectly expected globally granted reads to be denied and the implicit-deny simulation lacked the exact `aws:ResourceAccount` context requested by unrelated condition-bearing policies. Its state/lock absence checks passed, but Terraform init/plan and artifact publication were skipped and no mutation occurred; never use either failed run as an apply source. The corrective preflight supplies only the account already encoded in every simulated ARN, still rejects any missing context with escaped key-name diagnostics, and keeps any in-account wildcard write grant visible as an unexpected `allowed` result. Merge this correction before dispatching a wholly fresh plan. Stop on any missing control grant, broader-than-reviewed access, attachment count above the account quota, unexpected new attachment, or residual missing context.
- [ ] Pre-rollout cost: review the standing charges for six Interface VPC endpoints across three Availability Zones and one ElastiCache Serverless cache, as documented in `terraform/control/README.md`. Both sandbox plan and apply dispatches require the exact independent confirmation `ACCEPT_SANDBOX_CONTROL_STANDING_COST`; the apply also requires `APPLY_SANDBOX_CONTROL_FOUNDATION`.
- [ ] Sandbox rollout: only `justin-layerv` may originate and trigger any `Control Sandbox First Apply` dispatch. Every credentialed job is bound to the GitHub `sandbox` environment so the OIDC subject is environment-scoped. Dispatch from `main` with operation `plan` and confirmation `PLAN_ONLY`. Review the immutable artifact and require exactly 41 creates, zero changes, and zero destroys. The plan contract rejects Terraform action invocations, every configuration provisioner, any known or unknown security-group rule, and drift in the reviewed dark VPC, subnet, KMS, endpoint-policy, or Redis fields. It also requires the exact configuration-resource map and every reviewed security-topology/encryption reference, including VPC membership, route associations, endpoint and Redis attachment, Flow Logs, data-store KMS keys, and KMS aliases; forbidden IPv6, snapshot-restore, and password inputs must be absent from configuration rather than merely unknown in planned values. The read-only Terraform Plan (PR) lane runs that same strict checker against its freshly rendered real control plan, so provider or Terraform rendering drift fails on the introducing PR rather than at dispatch time. The workflow plan assumes the apply-capable sandbox role because refresh planning requires live reads; the actor gate, environment-scoped OIDC subject, and shared writer lock are its mutation boundary. A newly enabled region blocks the routing audit by design: audit that region's routing surfaces, then re-dispatch rather than weakening the global fail-closed check. The attended workflow shares `deploy-sandbox-infra`; during this one-time bootstrap a run may delay a regular sandbox deploy for the active operation's timeout (30 minutes for plan/verify or 60 minutes for apply). Then start a **separate apply dispatch** from the same main commit with the source run ID, planned commit SHA, saved-plan SHA-256, repeated cost acceptance, and exact apply confirmation. The apply validates that the source run completed successfully on `main`, verifies artifact hashes and provenance before requesting AWS credentials, enforces a two-day plan age, reruns every live preflight, re-renders and rechecks the hash-verified binary plan, and applies only that saved plan. The binary plan is Terraform-version and state-lineage sensitive; this cross-run pattern is restricted to the first apply by the pinned Terraform version/lock file, absent-state gate, and exact contract checks, and must not be reused after bootstrap. Both plan and apply must prove bucket versioning is `Enabled`; every credentialed job re-reads live `refs/heads/main` before AWS access, removes the persisted checkout credential immediately after its final authenticated ref fetch and before Terraform executes, and apply repeats that proof and credential removal immediately before `terraform apply`. Main advancement, artifact mismatch, an existing state/lock object, disabled versioning, or expiry requires a fresh plan. The GitHub `sandbox` environment currently has no protection rules, so the actor restriction plus second deliberate dispatch is the approval boundary; do not collapse plan and apply into one run. After the verified first apply, retire the create-only PR-plan checker invocation (or replace it with the separately reviewed no-op/update policy) in the same cleanup that retires this first-apply workflow; leaving the create-only gate behind would intentionally block later Control changes.
- [ ] Immediate sandbox cleanup: before ending the attended successful first-apply session, open the cleanup PR that removes the temporary first-apply workflow; removes the exact `ControlFirstApplySelfSimulation` statement from the existing `terraform_read` policy unless the same PR introduces a separately reviewed successor workflow that continues to run its exact self-role preflight; removes the strict create-only checker step from `.github/workflows/terraform-plan-pr.yml`; and updates or removes its `tests/scripts/test_check_control_sandbox_first_apply.py` workflow-lock assertion plus the obsolete create-only fixture and contract as appropriate. Alternatively, replace those create-only surfaces together with the separately reviewed no-op/update policy. Until that cleanup merges, unrelated Terraform PRs intentionally remain red.
- [ ] Sandbox verification: require `nhp/sandbox/control/terraform.tfstate` to be versioned and SSE-KMS encrypted by the exact reviewed state key, then require a refresh-enabled no-op plan and exact 41-resource state inventory. Confirm no function, Hub, listener, load balancer, DNS record, internet/NAT/peering/transit route, endpoint ingress, or non-deny endpoint policy exists. Require all six Interface services and the DynamoDB gateway endpoint to remain deny-all; require the VPC Flow Log to report `ACTIVE`/`SUCCESS` to the declared KMS-encrypted log group with no delivery error; and require ElastiCache Serverless engine major `7`, the exact user group, IAM authority user, disabled no-password `default` user (`off ~* -@all`), and namespaced ACL. Operation `verify` is not partial-apply recovery: it requires the Terraform state to be fully converged already. Use it with `VERIFY_SANDBOX_CONTROL_FOUNDATION` only when apply completed but post-apply verification or OTP seeding was interrupted; it does not create Terraform infrastructure, but it may seed the OTP pepper when and only when the existing secret has no versions, then must converge to the same checks. Triage a red apply run by step: if `terraform apply` succeeded and only a later verification or OTP-seed step failed, use `verify`; if `terraform apply` failed or the state is not converged, follow the partial-apply stop path below.
- [ ] Partial-apply stop path: if `terraform apply` exits non-zero after creating the Control state object, stop and preserve the run evidence. Do **not** rerun either `apply` or `verify`: the immutable first-apply path intentionally rejects existing state and verify intentionally rejects non-converged state. Require a separately reviewed manual convergence/recovery procedure before any further mutation.
- [ ] Production rollout: after sandbox evidence is clean, create a separate reviewed production-only plan/apply workflow for `nhp/prod/control/terraform.tfstate` under the production approval and concurrency boundary. Do not add an environment selector, production role, production state path, or production root to `Control Sandbox First Apply`; it is permanently bound to sandbox and cannot target production.
- [ ] Post-rollout state: run a refresh-enabled plan through the reviewed apply path in each environment and record zero unexpected drift. Confirm specifically that each DynamoDB gateway-endpoint prefix-list route remains endpoint-owned and does not appear as a known inline `aws_route_table.route` value or falsely trip the plan contract. The PR plan's `-refresh=false` mode is a configuration gate, not live-drift evidence.
- [ ] Post-rollout secret: the sandbox apply/verify workflow idempotently seeds exactly one 48-character random OTP pepper only when the secret has no versions. `PutSecretValue` uses the stable SHA-256 of the exact secret ARN as its 64-character `ClientRequestToken`, so a lost response followed by a new random payload fails closed on the same version ID instead of creating a second version. Because the token is deterministic for the ARN, a same-ARN prior version or ambiguous/no-`AWSCURRENT` history will never generate a different accepted candidate: stop for operator repair. If policy calls for full secret recreation, that changes the ARN and therefore the token. Verification requires exactly the deterministic version labeled only `AWSCURRENT`. It never calls `GetSecretValue`, prints the value, or stores it in Terraform. Repeat this fail-closed pattern in the future production-only workflow.
- [ ] Cross-repo: complete the no-delete/no-dual-ownership SES sender transfer in nhp#3273 before publishing any OTP authority runtime.
- [ ] Pre-runtime endpoints: inventory every AWS SDK/service call made by each runtime, including whether any call requires STS, and compare that inventory with the declared endpoint set. Add any missing private endpoint before activation; do not rely on these no-default-route subnets reaching a public AWS endpoint. Replace each deny-all endpoint policy and add its exact caller-to-endpoint security-group ingress in the same reviewed change. Private DNS is already enabled, so in-VPC AWS SDK calls resolve to these endpoints and fail if only one side of that boundary is activated.
- [ ] Pre-runtime observability: before runtime promotion, make one controlled private-endpoint call from the runtime canary and verify its flow record lands in the declared KMS-encrypted log group without a Flow Logs delivery error; this proves service-backplane log delivery is unaffected by the deny-all customer endpoint policies.
- [ ] Pre-runtime: prove every OTP/rate-limit key is `connector:`-prefixed as required by the `~connector:*` ACL, and prove the implementation still requires Redis scripting before granting endpoint ingress. If atomic operations no longer require it, remove `+@scripting`; otherwise document the exact namespaced scripts and retain the key boundary.
- [ ] Pre-runtime KMS: enumerate which authority calls use KMS directly and grant only those per-key actions (for example `kms:Sign` on the qat1 key and decrypt access for CMK-encrypted secrets). Do not grant blanket `kms:Decrypt`/`kms:GenerateDataKey` merely because DynamoDB, ElastiCache, ECR, or CloudWatch Logs use the data CMK; verify each managed service's grant/service-principal path separately.
- [ ] Lifecycle: keep the plan contract's delete/replacement rejection after first apply. Any required ForceNew change must carry a separately reviewed, resource-specific no-data-loss rollout and a narrow contract adjustment; never disable the destructive gate for a routine Terraform change.
- [ ] Rollback: if verification fails, stop before runtime publication and leave the unused foundation dark; do not reuse cell0 state or delete production-protected identity/authority tables.
