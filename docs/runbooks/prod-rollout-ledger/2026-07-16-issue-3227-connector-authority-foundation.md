# 2026-07-16 · Issue #3227 · Connector Authority foundation

- **Owner:** prod rollout coordinator
- **Source:** [nhp#3227](https://github.com/layervai/nhp/issues/3227), [SES transfer nhp#3273](https://github.com/layervai/nhp/issues/3273)

Create the dark, environment-global Control state before any Hub or authority
runtime is published. Keep it isolated and fail closed throughout rollout.

- [ ] Pre-rollout routing: the sandbox portion is complete. The `Control Sandbox First Apply` plan and apply dispatches both ran `scripts/check-control-global-routing.sh` against `10.102.0.0/16`. It enumerated every enabled region, invoked the regional overlap checker in each, and separately rejected nondeleted Direct Connect gateways, associations, or pending proposals. Plan run `29658285646` failed closed before Terraform preflight/init/plan because the apply role lacked `directconnect:DescribeConnections`; apply and verify were skipped, and the run produced no artifact or mutation. #3336 added the exact five-action `DirectConnectRead` expansion to the existing sandbox `terraform_read` policy, and normal main run `29664524687` deployed it in place. Plan run `29664879328` subsequently passed all 17 enabled-region routing audits plus the global Direct Connect gateway inventory. The association and proposal reads are fail-closed source-covered but correctly conditional on a nondeleted gateway; the live audit found none and therefore did not invoke those two APIs. Do not rerun or apply from either failed plan run. Keep this item open for production: `10.202.0.0/16` requires an equivalent separately reviewed production workflow, and the sandbox workflow cannot target production. Stop rather than bypass the production check if any routing surface cannot be proven empty.
- [ ] Pre-rollout PrivateLink: immediately before each environment's first apply, query EC2 endpoint-service discovery in that target account and region and verify `com.amazonaws.<region>.email` exists as an Interface service in every selected Availability Zone. Stop if it is absent; never substitute the distinct `email-smtp` service.
- [ ] Pre-rollout IAM: explicitly accept the shared
  `nhp-<env>-github-actions` apply role's ElastiCache user/user-group lifecycle
  and tag actions, then simulate them against the planned
  `layerv-nhp-<env>-control-*` user and user-group ARNs. Against cell-scoped
  ARNs, require every write to remain `implicitDeny` and separately require the
  intentionally account-wide `Describe*`/`List*` Terraform reads to remain
  `allowed`; do not conflate read visibility with write confinement. Recheck
  managed-policy attachments: the 2026-07-16 live count was sandbox 10/10 and
  production 8/10, while the static worst-case is 10/10. The foundation grant
  updates the existing `terraform_apply_data` policy and the Direct Connect
  audit, self-simulation, and ElastiCache read corrections update the existing
  `terraform_read` policy; neither adds an attachment. Plan run `29664879328`
  failed closed before endpoint/KMS/state preflight, Terraform init, or plan
  because the role could not call `iam:SimulatePrincipalPolicy`; #3340 deployed
  the exact self-role-scoped correction through the normal sandbox writer.
  Fresh plan run `29667228195` then passed the complete routing audit and failed
  closed in IAM preflight because the checker incorrectly expected globally
  granted reads to be denied and the write simulation lacked required request
  context. Replacement plan run `29671598430` again failed closed in IAM
  preflight: the live apply role still lacked unconditional ElastiCache reads,
  while its shared policies requested `aws:RequestedRegion` plus five unrelated
  service-specific context keys. Both failed runs skipped Terraform init, plan,
  artifact publication, apply, and verify; their state/lock absence checks
  passed and no mutation occurred. Never use either failed run as an apply
  source. The correction supplies the exact `aws:ResourceAccount` and
  `aws:RequestedRegion` values, tolerates only the frozen missing-key set
  `cloudwatch:namespace`, `iam:AWSServiceName`, `iam:PassedToService`,
  `route53:ChangeResourceRecordSetsNormalizedRecordNames`, and
  `ssm:resourceTag/Environment` on the cell-write simulation, and still requires
  every exact write action/resource decision to be `implicitDeny`. Any missing
  global or unknown key and any actual write grant remains fatal. Merge this
  correction, then let the normal sandbox Terraform deployment update the live
  `terraform_read` policy. Before dispatching a wholly fresh Control plan,
  verify the live role permits all six required cell read evaluations, denies
  every exact cell write evaluation under the supplied global context, and
  preserves state/lock absence and deployment quiescence. Stop on any missing
  control grant, broader-than-reviewed access, attachment count above the
  account quota, unexpected new attachment, or residual disallowed missing
  context.
- [x] Sandbox pre-rollout cost: the one-time sandbox plan and apply dispatches recorded the exact independent `ACCEPT_SANDBOX_CONTROL_STANDING_COST` confirmation for six Interface VPC endpoints across three Availability Zones and one ElastiCache Serverless cache; apply separately recorded `APPLY_SANDBOX_CONTROL_FOUNDATION`. Repeat the cost review under the future production rollout rather than reusing the retired sandbox confirmation.
- [x] Sandbox rollout: the one-time `Control Sandbox First Apply` workflow ran only from `main`, under the sandbox environment and shared writer lock, with separate reviewed plan/apply dispatches and exact artifact, state, routing, IAM, and live-boundary checks. The partial-apply outcome and bounded recovery are recorded below. The foundation is now converged and independently verified, so this cleanup deletes the one-time workflow and leaves the read-only PR lane on the exact 41-resource no-op contract. Do not recreate or redispatch the retired workflow; any future Control mutation must introduce a separately reviewed update policy and tests.
- [x] Immediate sandbox cleanup: NHP #3351 merged as `035738cbc1bf7add6b72194229efc3a390c54256`, removing the temporary first-apply workflow and every first-apply/recovery-only path while retaining the exact 41-resource no-op PR-plan gate and permanent state/live-boundary/Redis/Flow-Log verifier. Its exact-head Control plan was zero-change. Normal main-push sandbox writer run `29706838356`, infrastructure job `88245829098`, then applied the reviewed `terraform_read` contraction through its checked saved plan; the full-stack writer also performed its independently guarded routine sandbox reconciliation (`1 added, 7 changed, 1 destroyed`), so do not misstate the live apply as IAM-only. The policy's default version advanced from `v12` to `v13` at `2026-07-19T23:00:31Z`; `v13` contains neither `ControlFirstApplySelfSimulation` nor any action equal to `iam:SimulatePrincipalPolicy`. A fresh policy simulation against `arn:aws:iam::767397897469:role/nhp-sandbox-github-actions`, for that same action and self-role resource, first returned `implicitDeny` with zero matched statements and requested context; repeating it with every requested context key supplied returned `implicitDeny`, zero matched statements, and zero missing context. Never recreate the retired workflow or self-simulation grant.
- [x] Sandbox verification: standalone run `29704029176` completed green against exact main `bb140ab33d1ccb295c800c4d7b479941571f4ad2`. Only the operation-contract and verification jobs ran; every apply and recovery job was skipped. Sanitized evidence proves exactly 41 managed resources plus the four reviewed data sources, a zero-create/no-op plan bound to frozen contract digest `a97f65c62d1a4c5a6e26f60ef95995b2bd31f24c8d7d5f18fa732fad1e9b4140`, encrypted and versioned state, the live dark boundary and active Flow Log, and the Redis contract. The versionless OTP secret received its first deterministic version exactly once without exposing the secret. Do not rerun the completed recovery plan or recovery apply.
- [x] Partial-apply recovery: original apply run `29673343567` created 40 of 41 managed resources, then failed because `CreateServerlessCache` also authorized the configured user group and the apply role lacked that dependent-resource grant. Recovery apply run `29701499041` at commit `b681499e7dd5e680b1e0e0969838d1c7c5d6a0b4` re-proved the exact preserved state and applied only the missing cache (`1 added, 0 changed, 0 destroyed`). #3348 corrected the verifier invocation and #3349 corrected the managed-versus-data-source inventory model. Never rerun the original apply, recovery plan, or recovery apply.
- [ ] Production rollout: after sandbox evidence is clean, create a separate reviewed production-only plan/apply workflow for `nhp/prod/control/terraform.tfstate` under the production approval and concurrency boundary. Immediately before each authenticated plan or apply, re-read and require the exact live `refs/heads/main` commit, then remove any persisted checkout credential before Terraform executes; the production workflow must preserve these fail-closed ref and credential boundaries even though their one-time sandbox helpers are retired. Do not restore or extend the retired `Control Sandbox First Apply` workflow with an environment selector, production role, production state path, or production root.
- [ ] Post-rollout state: run a refresh-enabled plan through the reviewed apply path in each environment and record zero unexpected drift. Confirm specifically that each DynamoDB gateway-endpoint prefix-list route remains endpoint-owned and does not appear as a known inline `aws_route_table.route` value or falsely trip the plan contract. The PR plan's `-refresh=false` mode is a configuration gate, not live-drift evidence.
- [ ] Post-rollout secret: the completed sandbox verification seeded exactly one 48-character random OTP pepper because the secret had no versions. `PutSecretValue` used the stable SHA-256 of the exact secret ARN as its 64-character `ClientRequestToken`, so a lost response followed by a new random payload would fail closed on the same version ID instead of creating a second version. The permanent verifier requires exactly that deterministic version labeled only `AWSCURRENT`; it never calls `GetSecretValue`, prints the value, or stores it in Terraform. Repeat this fail-closed pattern in the future production-only workflow before checking this cross-environment item complete.
- [ ] Cross-repo: complete the no-delete/no-dual-ownership SES sender transfer in nhp#3273 before publishing any OTP authority runtime.
- [ ] Pre-runtime Hub recovery composition: after the assignment-only Hub executable/image and the separately reviewed NHP #3352 recovery-issue and #3353 recovery-completion adapters have merged, add recovery handling to the Hub process in its own narrow PR. Bind only the exact recovery alias/capability, keep recovery completion cell-scoped, and re-run the closed UDP wire, IAM, cancellation, replay, and no-HTTP gates before any listener becomes reachable. Do not stack recovery composition into the assignment-only process slice or activate it from adapter branches.
- [ ] Pre-runtime Hub observability: replace the assignment process's intentional no-op `WorkerObserver` with a reviewed, concurrent, nonblocking aggregate metrics implementation before any Hub listener becomes reachable. Export only the worker's closed outcome/classification/rejection enums and unlabeled challenge-size histograms; never attach source address, peer key, credential, request body, or other attacker-controlled labels or logs. Alarm at minimum on replay-capacity exhaustion, aggregate/peer shedding, response-queue rejection, write failure, Authority invocation/response failures, and deadline pressure, and prove those alarms from a private canary. The assignment-only process/image may land dark with the no-op observer but is not activation-ready.
- [ ] Pre-runtime endpoints: inventory every AWS SDK/service call made by each runtime, including whether any call requires STS, and compare that inventory with the declared endpoint set. Add any missing private endpoint before activation; do not rely on these no-default-route subnets reaching a public AWS endpoint. Replace each deny-all endpoint policy and add its exact caller-to-endpoint security-group ingress in the same reviewed change. Private DNS is already enabled, so in-VPC AWS SDK calls resolve to these endpoints and fail if only one side of that boundary is activated.
- [ ] Pre-runtime Hub credential path: the assignment process uses `awsconfig.LoadDefaultConfig` and synchronous Lambda Invoke. Deploy it with a direct task/instance role whose credentials come from the local ECS credential endpoint or IMDS, plus the private Lambda interface endpoint; do not set web-identity, assume-role, SSO, or shared-profile role configuration unless the inventory adds and policy-restricts the resulting STS/SSO dependency. Prove credential retrieval and both exact `:active` alias invocations from a no-default-route canary before enabling the public UDP target group.
- [ ] Pre-runtime observability: before runtime promotion, make one controlled private-endpoint call from the runtime canary and verify its flow record lands in the declared KMS-encrypted log group without a Flow Logs delivery error; this proves service-backplane log delivery is unaffected by the deny-all customer endpoint policies.
- [ ] Pre-runtime: prove every OTP/rate-limit key is `connector:`-prefixed as required by the `~connector:*` ACL, and prove the implementation still requires Redis scripting before granting endpoint ingress. If atomic operations no longer require it, remove `+@scripting`; otherwise document the exact namespaced scripts and retain the key boundary.
- [ ] Pre-runtime KMS: enumerate which authority calls use KMS directly and grant only those per-key actions (for example `kms:Sign` on the qat1 key and decrypt access for CMK-encrypted secrets). Do not grant blanket `kms:Decrypt`/`kms:GenerateDataKey` merely because DynamoDB, ElastiCache, ECR, or CloudWatch Logs use the data CMK; verify each managed service's grant/service-principal path separately.
- [ ] Lifecycle: keep the plan contract's delete/replacement rejection after first apply. Any required ForceNew change must carry a separately reviewed, resource-specific no-data-loss rollout and a narrow contract adjustment; never disable the destructive gate for a routine Terraform change.
- [ ] Rollback: if verification fails, stop before runtime publication and leave the unused foundation dark; do not reuse cell0 state or delete production-protected identity/authority tables.
