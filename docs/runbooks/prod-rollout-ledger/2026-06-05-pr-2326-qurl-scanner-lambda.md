# 2026-06-05 · PR #2326 · qurl-scanner Lambda + EventBridge + IAM

- **Owner:** prod rollout coordinator
- **Source:** [#2326](https://github.com/layervai/nhp/pull/2326) · [layervai/qurl-service#852](https://github.com/layervai/qurl-service/pull/852)

Adds the qurl-scanner Lambda + cron + alarms behind `qurl_scanner_lambda_enabled` (default off). Not yet in prod: ships disabled, rolled out via a two-apply (flag off → flag on) sequence per env after qurl-service CI publishes an image. See the PR for full preconditions.

- [x] Pre-rollout: confirm qurl-service `main` has published a `layerv/qurl-scanner-lambda` image to ECR and written its SHA to `/<name_prefix>/qurl-scanner-lambda-image-tag` SSM (Lambda `package_type = "Image"` fails create against an empty repo). _Sandbox: SSM `/layerv-nhp-sandbox/qurl-scanner-lambda-image-tag` = `7791fcc` at 2026-06-12T00:55:45Z, written by qurl-service `build-and-deploy` run [27386246407](https://github.com/layervai/qurl-service/actions/runs/27386246407) post-#914 merge._
- [x] Pre-rollout: confirm qurl-service branch protection requires the `Docker Build (scanner-lambda)` job before merge to main. _Done 2026-06-17: updated qurl-service `main` required status checks; readback includes `Docker Build (scanner-lambda)` alongside `Lint`, `Test`, `Build`, `Docker Build`, and age checks._
- [ ] Rollout (prod phase 1): run the Terraform flag-off apply that creates/manages the prod SSM image-tag parameter (`/layerv-nhp-prod/qurl-scanner-lambda-image-tag`), then set or verify it points at a replication-verified scanner SHA before the later flag-on apply. qurl-service CI only writes the sandbox path; do not ad hoc-create the prod parameter before Terraform owns it. _Current 2026-06-17: prod parameter is still absent (`ParameterNotFound`); this is a normal prod Terraform rollout item, not a pre-release shell write._
- [x] Pre-rollout (HARD PROD): confirm ECR replication propagated the image to the prod (secondary) account/region — `aws ecr describe-images --repository-name layerv/qurl-scanner-lambda --image-ids imageTag=<SHA>` returns the same digest (Lambda cannot pull cross-account; wait ~5 min for replication lag). _Done 2026-06-17: sandbox/prod ECR both resolve `cef5f89` to digest `sha256:1fb7f3b215da0f9cb1b8fbff5cbfe207ded6a2c712d0c19dac63e0cbc48fa3d4`; prod push timestamp 2026-06-16T08:26:22Z._
- [x] Pre-rollout: `aws lambda get-account-settings` → `UnreservedConcurrentExecutions` ≥ 101 before flipping (reserved-concurrency=1 subtracts from the pool; request a quota bump if near 100). _Sandbox: `UnreservedConcurrentExecutions = 996` at 2026-06-12T01:11Z._
- [x] Rollout: per env, first apply with flag off (creates ECR repo + SSM param), then second apply with `qurl_scanner_lambda_enabled = true` (creates Lambda + EventBridge cron + scan-gap alarm). Prod repeats the sequence; gated by qurl-service task #85 (SQS/consumer/`--allow-prod-emit`). _Sandbox: first apply landed 2026-06-05 with #2326; second apply landed 2026-06-12T01:27Z via dispatched run [27388149649](https://github.com/layervai/nhp/actions/runs/27388149649) after the IAM-em-dash (#2461) + Lambda image-format (#914) blockers cleared. Prod still on first apply (flag off in prod tfvars)._
- [x] Rollout: entrypoint smoke — `aws lambda invoke --function-name layerv-nhp-sandbox-cell0-qurl-scanner` and confirm `scanner starting` + `scanner tick complete` logs (empty bucket, no panic). _Sandbox: 200 OK at 2026-06-12T01:55Z; logs show `scanner starting` + `scanner tick complete` with `emit_errors=0, fence_errors=0` on `e34abcf7-6039-4e87-a4cb-22162f8e6688`._
- [x] Rollout (REQUIRED BEFORE PROD FLAG FLIP): data-path smoke — mint a short-expiry transit qURL, then operator-replay invoke with `--payload '{"bucket": <int>}'`; confirm GSI Query returned items, UpdateItem succeeded (no KMSAccessDeniedException / IAM denial), `scanner_errors_burning` stayed OK. _Sandbox: first scheduled tick at 2026-06-12T01:51:43Z found 10 real expired qURLs in bucket 29687150 (scanned=10, emit_errors=0, fence_errors=0). KMS:Decrypt against secrets CMK works at runtime; load-bearing cr round-4 gate is green on real data._
- [x] Post-rollout: confirm scan-gap alarm `layerv-nhp-<env>-cell0-qurl-scanner-invocation-gap` is OK (fires on `Sum(Invocations) ≤ 3 over 5 min × 2 periods`). _Sandbox: `OK` at 2026-06-12T01:55Z (4 invocations in last 5-min window vs ≤3 threshold). `scanner-errors-burning` also `OK` (0 errors vs ≥1 threshold)._
- [x] Post-rollout: confirm log group `/aws/lambda/layerv-nhp-<env>-cell0-qurl-scanner` is KMS-encrypted with 30-day retention, and the Lambda is in log-only emit mode (no `EMIT_MODE` env var, no `--allow-prod-emit`). _Sandbox: log group `KmsKeyId=arn:aws:kms:us-east-2:767397897469:key/c5250da7-1d0f-40a7-9ee5-64699fe15e10`, `RetentionDays=30`. Lambda env vars omit `EMIT_MODE` and `--allow-prod-emit`; live logs confirm `emit_mode: log-only`._
- [ ] Rollback: flip `qurl_scanner_lambda_enabled = false` and re-apply (destroys Lambda + cron + alarm; leaves ECR repo + SSM param). Per-event kill: unset `EMIT_MODE` (or set `log-only`) and re-apply.
- [ ] Cross-repo follow-ups: [qurl-service#853](https://github.com/layervai/qurl-service/issues/853) (memoize AWS clients), [qurl-service#854](https://github.com/layervai/qurl-service/issues/854) (retire ECR-probe scaffolding). On SQS-activation PR, thread the queue KMS key ARN + add `kms:GenerateDataKey`/`kms:Decrypt` to `scanner_lambda_sqs`; audit binary error handling before wiring `scanner_lambda_alarm_sns_topic_arn` to a paging topic (see PR). _Wiring landed in #2491 (root → `module.monitoring.sns_topic_arn`, both envs); the error-handling audit is satisfied by the sandbox burn-in above — scan-gap + `scanner-errors-burning` both OK, `emit_errors=0`/`fence_errors=0` across real ticks. #853/#854 stay open._
