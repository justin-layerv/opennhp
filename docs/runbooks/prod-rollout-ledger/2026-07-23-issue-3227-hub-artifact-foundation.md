# 2026-07-23 · Issue #3227 · Hub artifact foundation

- **Owner:** prod rollout coordinator
- **Source:** [issue #3227](https://github.com/layervai/nhp/issues/3227), [PR #3399](https://github.com/layervai/nhp/pull/3399), [PR #3403](https://github.com/layervai/nhp/pull/3403)

Create the dark Hub artifact boundary before any Hub image publication or runtime. Keep the build matrix `publish: false` throughout this rollout.

- [x] Pre-rollout: after #3399 and #3403 merged, read-only Control verify [run 30048869366](https://github.com/layervai/nhp/actions/runs/30048869366) at exact main `0bf3cb72653e15cfa0efdde2101e279e59524f1b` proved a refresh-enabled exact 45-managed/four-data-resource no-op with state serial 9/version `pNlrumzA9RgLlnLQJ6cS3rSZtskkLeK2`, zero normalization drift, and the live dark boundary. Same-account (`767397897469`) ECR, SSM, and IAM readback immediately afterward returned `RepositoryNotFoundException`, `ParameterNotFound`, and `NoSuchEntity` for `layerv/nhp-hub`, `/sandbox/nhp/control/hub/image-digest`, and `layerv-nhp-sandbox-control-hub-publisher`.
- [x] Publication-trust prerequisite: on 2026-07-23, live GitHub settings readback proved that `hub-publish-sandbox` and `hub-publish-production` both have required reviewer Justin (`178750268`), a sole custom `main` deployment branch policy, `can_admins_bypass=true`, and `prevent_self_review=false`; shared `sandbox` and `production` were unchanged. This was completed before applying the publisher role, so publication remained impossible while either dedicated Environment was absent or unprotected.
- [ ] Rollout: review and apply one saved sandbox plan containing only the five Hub artifact creates plus 45 no-ops. If the provider reflects the separately managed policy onto the role, apply only the checker's exact `hub-publisher-role` refresh normalization. Record the resulting exact 50-resource no-op.
- [ ] Post-rollout: prove the repository is KMS-encrypted, immutable, scan-on-push, protected from destroy, and expires only untagged uploads after seven days; prove the digest pin is `UNPUBLISHED`. Simulate exact publisher allows for only `layerv/nhp-hub` and its digest pin, and denials for other repositories, parameters, Control data, and runtime actions. Prove the role admits only `repo:layervai/nhp:environment:hub-publish-sandbox` and rejects the shared `sandbox` Environment subject. Do not configure the publisher-role secret yet.
- [ ] Cross-repo carrier: only after the publication-trust prerequisite passes, a later reviewed NHP carrier may switch only the Hub matrix row from `publish: false`, declare `hub-publish-sandbox`, assume the dedicated role, publish only the source SHA, prove the canonical manifest digest and scan result through the read-only `DescribeImageScanFindings` grant, write/read back the lowercase `sha256:<64hex>` pin, and converge any exact `hub-digest` refresh normalization before runtime rollout. Before every publication attempt, the carrier must live-read the exact selected Environment and fail closed unless required reviewer Justin (`178750268`) remains configured and its only deployment branch policy is custom `main`; it must also fail closed if configured with the shared `sandbox` or `production` Environment. The 2026-07-23 readback is rollout evidence, not a substitute for this runtime preflight.
- [ ] Production: do not apply from the sandbox contract. After production foundation gates close, review a separate saved plan with the production OIDC subject `repo:layervai/nhp:environment:hub-publish-production`, environment-specific ARNs, and the then-current full inventory. Production publication remains disabled until the dedicated `hub-publish-production` live protection readback above passes independently.
- [ ] Rollback: disable publication by removing the carrier's role secret or reverting its publish gate. Preserve the immutable repository and digest pin; do not destroy or orphan either artifact resource while a runtime or rollback may reference it.
